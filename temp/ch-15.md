# 📘 JavaScript Complete Book — Chapter 15

# DOM — Document Object Model

### HTML-এর সাথে JavaScript-এর যোগাযোগ

এখন থেকে JavaScript-এর সবচেয়ে গুরুত্বপূর্ণ practical অংশগুলোর একটি শুরু হচ্ছে — **DOM (Document Object Model)**।

এতদিন আমরা JavaScript-এর ভিতরের language concepts শিখেছি। এখন শিখব কীভাবে JavaScript দিয়ে **HTML webpage-এর content, structure, style এবং elements পরিবর্তন করা যায়।**

---

# 15.1 DOM কী? — DOM কীভাবে কাজ করে?

**DOM = Document Object Model**

Browser যখন একটি HTML page load করে, তখন HTML document-কে একটি **object-based tree structure** হিসেবে তৈরি করে।

ধরো HTML:

```html
<!DOCTYPE html>
<html>
<head>
    <title>My Page</title>
</head>

<body>

    <h1>Hello World</h1>

    <p>Welcome to JavaScript</p>

    <button>Click Me</button>

</body>
</html>
```

Browser এটাকে roughly এভাবে চিন্তা করে:

```text
Document
│
└── html
    │
    ├── head
    │   └── title
    │
    └── body
        │
        ├── h1
        │
        ├── p
        │
        └── button
```

এই structure-টাই DOM Tree।

---

# 15.2 DOM-এর Real-Life Example

ধরো একটি University campus:

```text
University
│
├── CSE Department
│   ├── Teacher
│   ├── Student
│   └── Lab
│
└── EEE Department
    ├── Teacher
    └── Lab
```

এখানে:

* University = root
* Department = parent
* Teacher/Student/Lab = child

HTML DOM-ও একই ধরনের hierarchy follow করে।

---

# 15.3 JavaScript কেন DOM ব্যবহার করে?

ধরো HTML:

```html
<h1>Hello</h1>
```

JavaScript দিয়ে তুমি এটাকে পরিবর্তন করতে পারো:

```javascript
document.querySelector("h1").textContent = "Welcome Shariar!";
```

Browser-এ:

```text
Welcome Shariar!
```

অর্থাৎ:

```text
HTML
 ↓
DOM
 ↓
JavaScript
 ↓
Change
 ↓
Updated Webpage
```

---

# 15.4 `document` কী?

DOM-এর সবচেয়ে গুরুত্বপূর্ণ object হলো:

```javascript
document
```

`document` পুরো HTML webpage-কে represent করে।

Example:

```javascript
console.log(document);
```

Browser console-এ পুরো HTML document দেখতে পারবে।

---

# 15.5 `document.documentElement`

পুরো HTML element পাওয়া যায়:

```javascript
console.log(document.documentElement);
```

Output:

```html
<html>...</html>
```

---

# 15.6 `document.head`

HTML-এর `<head>`:

```javascript
console.log(document.head);
```

Output:

```html
<head>...</head>
```

---

# 15.7 `document.body`

HTML-এর `<body>`:

```javascript
console.log(document.body);
```

Output:

```html
<body>...</body>
```

---

# 15.8 DOM Element কী?

HTML-এর প্রতিটি element DOM-এর মধ্যে একটি object/node হিসেবে থাকে।

যেমন:

```html
<h1>Hello</h1>
```

এখানে:

```text
h1 = DOM Element
```

JavaScript:

```javascript
let heading = document.querySelector("h1");

console.log(heading);
```

---

# 15.9 DOM Node কী?

DOM শুধু HTML element নিয়ে তৈরি নয়।

DOM-এ বিভিন্ন ধরনের node থাকতে পারে।

যেমন:

```text
Document
Element
Text
Comment
Attribute
```

Example:

```html
<h1>Hello</h1>
```

এখানে:

```text
h1 → Element Node
Hello → Text Node
```

---

# 15.10 DOM Tree

ধরো:

```html
<body>

    <h1>Hello</h1>

    <p>JavaScript</p>

</body>
```

DOM Tree:

```text
document
   │
   └── html
       │
       └── body
           │
           ├── h1
           │    └── "Hello"
           │
           └── p
                └── "JavaScript"
```

---

# 15.11 Element Select করা

DOM manipulation-এর প্রথম কাজ হলো:

> HTML element-কে JavaScript দিয়ে খুঁজে বের করা।

সবচেয়ে গুরুত্বপূর্ণ methods:

```javascript
document.getElementById()
document.getElementsByClassName()
document.getElementsByTagName()
document.querySelector()
document.querySelectorAll()
```

---

# 15.12 `getElementById()`

HTML:

```html
<h1 id="title">Hello World</h1>
```

JavaScript:

```javascript
let title = document.getElementById("title");

console.log(title);
```

এটি `<h1>` element return করবে।

---

# 15.13 ID দিয়ে Content Change

HTML:

```html
<h1 id="title">Hello World</h1>
```

JavaScript:

```javascript
let title = document.getElementById("title");

title.textContent = "Welcome to JavaScript!";
```

Browser:

```text
Welcome to JavaScript!
```

---

# 15.14 `getElementsByClassName()`

HTML:

```html
<p class="text">One</p>
<p class="text">Two</p>
<p class="text">Three</p>
```

JavaScript:

```javascript
let elements = document.getElementsByClassName("text");

console.log(elements);
```

এখানে multiple elements পাওয়া যাবে।

এটি একটি **HTMLCollection** return করে।

---

# 15.15 HTMLCollection

```javascript
let elements = document.getElementsByClassName("text");

console.log(elements.length);
```

Output:

```text
3
```

Index দিয়ে access:

```javascript
console.log(elements[0]);
```

প্রথম `<p>` পাওয়া যাবে।

---

# 15.16 `getElementsByTagName()`

Tag name দিয়ে element খুঁজতে:

```html
<p>Hello</p>
<p>JavaScript</p>
<p>DOM</p>
```

```javascript
let paragraphs = document.getElementsByTagName("p");

console.log(paragraphs.length);
```

Output:

```text
3
```

---

# 15.17 `querySelector()`

এটি DOM-এর সবচেয়ে useful selector method-গুলোর একটি।

```javascript
document.querySelector(selector);
```

### ID:

```javascript
let title = document.querySelector("#title");
```

### Class:

```javascript
let box = document.querySelector(".box");
```

### Tag:

```javascript
let heading = document.querySelector("h1");
```

---

# 15.18 `querySelector()` প্রথম element দেয়

HTML:

```html
<p class="text">One</p>
<p class="text">Two</p>
<p class="text">Three</p>
```

```javascript
let element = document.querySelector(".text");

console.log(element.textContent);
```

Output:

```text
One
```

শুধু প্রথম matching element পাওয়া যাবে।

---

# 15.19 `querySelectorAll()`

সব matching element পেতে:

```javascript
let elements = document.querySelectorAll(".text");

console.log(elements);
```

এটি সাধারণত একটি **NodeList** return করে।

---

# 15.20 NodeList

```javascript
let elements = document.querySelectorAll(".text");

console.log(elements.length);
```

Output:

```text
3
```

Index:

```javascript
console.log(elements[1].textContent);
```

Output:

```text
Two
```

---

# 15.21 `querySelector()` বনাম `querySelectorAll()`

| Method               | Result                 |
| -------------------- | ---------------------- |
| `querySelector()`    | প্রথম matching element |
| `querySelectorAll()` | সব matching element    |

Example:

```javascript
document.querySelector(".box");
```

→ প্রথম `.box`

```javascript
document.querySelectorAll(".box");
```

→ সব `.box`

---

# 15.22 CSS Selector ব্যবহার

`querySelector()`-এ CSS selector ব্যবহার করা যায়।

HTML:

```html
<div id="container">
    <p class="text">Hello</p>
</div>
```

### ID

```javascript
document.querySelector("#container");
```

### Class

```javascript
document.querySelector(".text");
```

### Tag

```javascript
document.querySelector("p");
```

### Descendant

```javascript
document.querySelector("#container p");
```

---

# 15.23 Multiple Selector

```javascript
let element = document.querySelector("h1, p");
```

প্রথম matching `h1` অথবা `p` পাওয়া যাবে।

---

# 15.24 Attribute Selector

HTML:

```html
<input type="email">
```

JavaScript:

```javascript
let input = document.querySelector('input[type="email"]');
```

---

# 15.25 Content পরিবর্তন — `textContent`

HTML:

```html
<h1 id="title">Old Title</h1>
```

JavaScript:

```javascript
document.getElementById("title").textContent = "New Title";
```

Browser:

```text
New Title
```

---

# 15.26 `innerHTML`

HTML:

```html
<div id="box"></div>
```

JavaScript:

```javascript
document.getElementById("box").innerHTML = "<h2>Hello</h2>";
```

এখন HTML হবে:

```html
<div id="box">
    <h2>Hello</h2>
</div>
```

---

# 15.27 `textContent` বনাম `innerHTML`

| `textContent`           | `innerHTML`                   |
| ----------------------- | ----------------------------- |
| Text set করে            | HTML set করতে পারে            |
| HTML parse করে না       | HTML parse করে                |
| সাধারণত safer           | User input-এর ক্ষেত্রে সাবধান |
| Plain text-এর জন্য ভালো | Dynamic HTML-এর জন্য useful   |

Example:

```javascript
element.textContent = "<b>Hello</b>";
```

Browser:

```text
<b>Hello</b>
```

কিন্তু:

```javascript
element.innerHTML = "<b>Hello</b>";
```

Browser:

**Hello** (bold)

---

# 15.28 `innerText`

```javascript
element.innerText
```

এটি user-visible text-এর সাথে বেশি সম্পর্কিত।

Example:

```javascript
let text = document.querySelector("p");

console.log(text.innerText);
```

### সাধারণভাবে মনে রাখো:

```text
textContent → DOM-এর text
innerText   → rendered/visible text-এর দিকে বেশি focused
innerHTML   → HTML structure সহ
```

---

# 15.29 Element-এর Attribute

HTML:

```html
<img id="photo" src="old.jpg" alt="Old">
```

JavaScript:

```javascript
let image = document.getElementById("photo");

console.log(image.src);
console.log(image.alt);
```

---

# 15.30 `getAttribute()`

```javascript
let image = document.querySelector("img");

console.log(image.getAttribute("src"));
```

Output:

```text
old.jpg
```

---

# 15.31 `setAttribute()`

```javascript
image.setAttribute("src", "new.jpg");
```

এখন:

```html
<img src="new.jpg">
```

---

# 15.32 `hasAttribute()`

```javascript
let image = document.querySelector("img");

console.log(image.hasAttribute("alt"));
```

Output:

```text
true
```

---

# 15.33 `removeAttribute()`

```javascript
image.removeAttribute("alt");
```

এখন `alt` attribute remove হয়ে যাবে।

---

# 15.34 Direct Property দিয়ে Attribute পরিবর্তন

অনেক common attribute সরাসরি property দিয়েও পরিবর্তন করা যায়।

```javascript
let image = document.querySelector("img");

image.src = "new.jpg";
image.alt = "New image";
```

---

# 15.35 Input Value

HTML:

```html
<input id="username" type="text">
```

JavaScript:

```javascript
let input = document.getElementById("username");

input.value = "Shariar";
```

Input box-এ:

```text
Shariar
```

দেখাবে।

Value পড়তে:

```javascript
console.log(input.value);
```

---

# 15.36 Checkbox

HTML:

```html
<input id="agree" type="checkbox">
```

JavaScript:

```javascript
let checkbox = document.getElementById("agree");

console.log(checkbox.checked);
```

Output:

```text
false
```

Check করলে:

```javascript
checkbox.checked = true;
```

---

# 15.37 Select Option

HTML:

```html
<select id="department">

    <option value="cse">CSE</option>
    <option value="eee">EEE</option>

</select>
```

JavaScript:

```javascript
let department = document.getElementById("department");

console.log(department.value);
```

---

# 15.38 CSS পরিবর্তন — `style`

HTML:

```html
<h1 id="title">Hello</h1>
```

JavaScript:

```javascript
let title = document.getElementById("title");

title.style.color = "red";
title.style.fontSize = "40px";
```

এখন heading লাল এবং বড় হবে।

---

# 15.39 CSS Property Naming

CSS:

```css
background-color: blue;
```

JavaScript:

```javascript
element.style.backgroundColor = "blue";
```

CSS:

```css
font-size: 30px;
```

JavaScript:

```javascript
element.style.fontSize = "30px";
```

CSS:

```css
margin-top: 20px;
```

JavaScript:

```javascript
element.style.marginTop = "20px";
```

অর্থাৎ CSS-এর `kebab-case` → JavaScript-এ `camelCase`।

---

# 15.40 `classList`

Direct style পরিবর্তনের চেয়ে অনেক সময় CSS class ব্যবহার করা ভালো।

HTML:

```html
<div id="box">Hello</div>
```

CSS:

```css
.active {
    background-color: blue;
    color: white;
}
```

JavaScript:

```javascript
let box = document.getElementById("box");

box.classList.add("active");
```

---

# 15.41 `classList.add()`

```javascript
element.classList.add("active");
```

একটি class যোগ করে।

---

# 15.42 `classList.remove()`

```javascript
element.classList.remove("active");
```

Class remove করে।

---

# 15.43 `classList.toggle()`

```javascript
element.classList.toggle("active");
```

যদি class থাকে:

```text
remove
```

না থাকলে:

```text
add
```

এটি খুব useful।

---

# 15.44 `classList.contains()`

```javascript
console.log(element.classList.contains("active"));
```

Output:

```text
true
```

অথবা:

```text
false
```

---

# 15.45 `classList.replace()`

```javascript
element.classList.replace("old", "new");
```

একটি class অন্য class দিয়ে replace করবে।

---

# 15.46 Element তৈরি করা

JavaScript দিয়ে নতুন HTML element তৈরি করা যায়।

```javascript
let paragraph = document.createElement("p");
```

এখন `paragraph` একটি নতুন DOM element।

---

# 15.47 নতুন Element-এর Content

```javascript
let paragraph = document.createElement("p");

paragraph.textContent = "Hello JavaScript";
```

কিন্তু এখনও webpage-এ দেখা যাবে না।

কারণ element-টাকে DOM-এ add করতে হবে।

---

# 15.48 `append()`

HTML:

```html
<div id="container"></div>
```

JavaScript:

```javascript
let container = document.getElementById("container");

let paragraph = document.createElement("p");

paragraph.textContent = "Hello JavaScript";

container.append(paragraph);
```

DOM:

```html
<div id="container">
    <p>Hello JavaScript</p>
</div>
```

---

# 15.49 `appendChild()`

```javascript
container.appendChild(paragraph);
```

এটিও child element add করে।

---

# 15.50 `append()` বনাম `appendChild()`

| `append()`                    | `appendChild()`     |
| ----------------------------- | ------------------- |
| Node এবং text দুটোই নিতে পারে | Node নেয়            |
| একাধিক item নিতে পারে         | সাধারণত একটি node   |
| Modern ও flexible             | Traditional DOM API |

Example:

```javascript
container.append("Hello");
```

এটা সম্ভব।

কিন্তু:

```javascript
container.appendChild("Hello");
```

এটা valid node না হওয়ায় কাজ করবে না।

---

# 15.51 `prepend()`

Parent-এর শুরুতে element যোগ করতে:

```javascript
container.prepend(paragraph);
```

---

# 15.52 `before()`

কোনো element-এর আগে কিছু যোগ করতে:

```javascript
element.before(paragraph);
```

---

# 15.53 `after()`

কোনো element-এর পরে:

```javascript
element.after(paragraph);
```

---

# 15.54 Element Remove

Modern method:

```javascript
element.remove();
```

Example:

```javascript
let paragraph = document.querySelector("p");

paragraph.remove();
```

---

# 15.55 `removeChild()`

Parent থেকে child remove:

```javascript
container.removeChild(paragraph);
```

---

# 15.56 Element Replace

```javascript
container.replaceChildren(paragraph);
```

এটি container-এর existing children replace করে।

আর একটি নির্দিষ্ট child replace করতে:

```javascript
oldElement.replaceWith(newElement);
```

---

# 15.57 Clone করা

একটি DOM element copy করতে:

```javascript
let copy = element.cloneNode(true);
```

এখানে:

```text
true → children-সহ clone
false → শুধু element clone
```

---

# 15.58 Parent খুঁজে বের করা

HTML:

```html
<div id="parent">
    <p id="child">Hello</p>
</div>
```

JavaScript:

```javascript
let child = document.getElementById("child");

console.log(child.parentElement);
```

`div` পাওয়া যাবে।

---

# 15.59 Children

```javascript
let parent = document.getElementById("parent");

console.log(parent.children);
```

এটি শুধু element children দেখায়।

---

# 15.60 `firstElementChild`

```javascript
console.log(parent.firstElementChild);
```

---

# 15.61 `lastElementChild`

```javascript
console.log(parent.lastElementChild);
```

---

# 15.62 `nextElementSibling`

HTML:

```html
<p>One</p>
<p id="current">Two</p>
<p>Three</p>
```

JavaScript:

```javascript
let current = document.getElementById("current");

console.log(current.nextElementSibling);
```

Output:

```html
<p>Three</p>
```

---

# 15.63 `previousElementSibling`

```javascript
console.log(current.previousElementSibling);
```

Output:

```html
<p>One</p>
```

---

# 15.64 DOM Traversal

DOM traversal মানে DOM Tree-এর মধ্যে এক element থেকে অন্য element-এ যাওয়া।

Important properties:

```javascript
parentElement
children
firstElementChild
lastElementChild
nextElementSibling
previousElementSibling
```

---

# 15.65 `children` বনাম `childNodes`

```javascript
element.children
```

→ শুধু element children।

```javascript
element.childNodes
```

→ text node, comment node সহ বিভিন্ন node থাকতে পারে।

Example:

```html
<div>
    Hello
    <p>World</p>
</div>
```

এখানে whitespace/text node থাকার কারণে `childNodes` এবং `children`-এর result একই নাও হতে পারে।

---

# 15.66 `closest()`

কোনো element-এর কাছের matching ancestor খুঁজতে:

```javascript
let button = document.querySelector("button");

let card = button.closest(".card");
```

Real-world application-এ এটি খুব useful।

---

# 15.67 `matches()`

কোনো element CSS selector match করছে কিনা:

```javascript
if (element.matches(".active")) {
    console.log("Active");
}
```

---

# 15.68 DOM Mini Project — Counter

এখন একটি ছোট project করি।

### HTML

```html
<h1 id="count">0</h1>

<button id="increase">+</button>
<button id="decrease">-</button>
```

### JavaScript

```javascript
let count = 0;

let countElement = document.getElementById("count");

let increaseButton =
    document.getElementById("increase");

let decreaseButton =
    document.getElementById("decrease");

increaseButton.addEventListener("click", function () {

    count++;

    countElement.textContent = count;

});

decreaseButton.addEventListener("click", function () {

    count--;

    countElement.textContent = count;

});
```

এখানে আমরা `addEventListener()` ব্যবহার করেছি, যেটা Chapter 16-এ বিস্তারিত শিখব।

---

# 15.69 DOM Mini Project — Dynamic List

HTML:

```html
<input id="taskInput" type="text">

<button id="addButton">Add Task</button>

<ul id="taskList"></ul>
```

JavaScript:

```javascript
let input = document.getElementById("taskInput");
let button = document.getElementById("addButton");
let list = document.getElementById("taskList");

button.addEventListener("click", function () {

    let task = input.value;

    if (task === "") {
        return;
    }

    let li = document.createElement("li");

    li.textContent = task;

    list.append(li);

    input.value = "";

});
```

User লিখবে:

```text
Learn JavaScript
```

তারপর Add চাপলে:

```text
• Learn JavaScript
```

এভাবে dynamic list তৈরি হবে।

---

# 15.70 DOM Mini Project — Dark Mode

HTML:

```html
<button id="themeButton">
    Toggle Dark Mode
</button>
```

CSS:

```css
.dark {
    background-color: black;
    color: white;
}
```

JavaScript:

```javascript
let button = document.getElementById("themeButton");

button.addEventListener("click", function () {

    document.body.classList.toggle("dark");

});
```

Button click করলে:

```text
Light → Dark
Dark → Light
```

---

# 15.71 `dataset`

HTML:

```html
<button
    id="product"
    data-id="101"
    data-category="laptop">
    Laptop
</button>
```

JavaScript:

```javascript
let product = document.getElementById("product");

console.log(product.dataset.id);
console.log(product.dataset.category);
```

Output:

```text
101
laptop
```

`data-*` attributes custom data রাখার জন্য useful।

---

# 15.72 DOM-এর কিছু গুরুত্বপূর্ণ Properties

| Property        | কাজ              |
| --------------- | ---------------- |
| `textContent`   | Text পড়া/লেখা    |
| `innerHTML`     | HTML পড়া/লেখা    |
| `innerText`     | Visible text     |
| `value`         | Input value      |
| `style`         | Inline CSS       |
| `classList`     | CSS class manage |
| `id`            | Element ID       |
| `children`      | Element children |
| `parentElement` | Parent           |
| `dataset`       | `data-*` values  |

---

# 15.73 DOM-এর গুরুত্বপূর্ণ Methods

| Method                     | কাজ                    |
| -------------------------- | ---------------------- |
| `getElementById()`         | ID দিয়ে select         |
| `getElementsByClassName()` | Class দিয়ে select      |
| `getElementsByTagName()`   | Tag দিয়ে select        |
| `querySelector()`          | প্রথম matching element |
| `querySelectorAll()`       | সব matching elements   |
| `createElement()`          | Element তৈরি           |
| `append()`                 | শেষে add               |
| `prepend()`                | শুরুতে add             |
| `remove()`                 | Remove                 |
| `replaceWith()`            | Replace                |
| `cloneNode()`              | Clone                  |
| `closest()`                | Ancestor খোঁজা         |
| `matches()`                | Selector match check   |
| `getAttribute()`           | Attribute পড়া          |
| `setAttribute()`           | Attribute set          |
| `removeAttribute()`        | Attribute remove       |
| `hasAttribute()`           | Attribute আছে কিনা     |

---

# ⚠️ 15.74 `innerHTML` ব্যবহারে সতর্কতা

ধরো user input:

```javascript
let userInput = "<img src=x onerror=alert('Hacked')>";
```

এটা সরাসরি:

```javascript
element.innerHTML = userInput;
```

দিলে security problem হতে পারে।

এ ধরনের issue-কে সাধারণভাবে **XSS (Cross-Site Scripting)** বলা হয়।

User-provided plain text দেখানোর ক্ষেত্রে:

```javascript
element.textContent = userInput;
```

সাধারণত safer approach।

Security chapter-এ আমরা XSS আরও বিস্তারিত দেখব।

---

# ⚡ 15.75 Static vs Live Collections

এটি একটু advanced কিন্তু গুরুত্বপূর্ণ।

`getElementsByClassName()` সাধারণত **live HTMLCollection** দেয়।

```javascript
let items = document.getElementsByClassName("item");
```

DOM পরিবর্তন হলে collection-এর content automatically update হতে পারে।

অন্যদিকে:

```javascript
let items = document.querySelectorAll(".item");
```

সাধারণত **static NodeList** দেয়।

অর্থাৎ পরে DOM-এ নতুন matching element যোগ করলেও আগের NodeList automatically update হবে না।

---

# 15.76 DOM Ready হওয়ার বিষয়

যদি JavaScript HTML element তৈরি হওয়ার আগেই execute করে:

```javascript
let title = document.getElementById("title");

console.log(title);
```

তাহলে `null` পাওয়া যেতে পারে।

একটি common solution হলো script-কে HTML-এর শেষে রাখা:

```html
<body>

    <h1 id="title">Hello</h1>

    <script src="script.js"></script>

</body>
```

আরেকটি approach হলো:

```javascript
document.addEventListener("DOMContentLoaded", function () {

    let title = document.getElementById("title");

    console.log(title);

});
```

`DOMContentLoaded` আমরা Events chapter-এ আরও বিস্তারিত দেখব।

---

# 🧠 15.77 DOM-এর পুরো Mental Model

একটি webpage-কে এভাবে চিন্তা করো:

```text
HTML
  ↓
Browser parses HTML
  ↓
DOM Tree তৈরি
  ↓
JavaScript DOM select করে
  ↓
Content / Attribute / Style / Structure change
  ↓
Browser updated UI দেখায়
```

---

# 🔥 15.78 Must Know

JavaScript দিয়ে frontend development করতে হলে এগুলো অবশ্যই ভালোভাবে জানতে হবে:

### Selection

```javascript
getElementById()
getElementsByClassName()
getElementsByTagName()
querySelector()
querySelectorAll()
```

### Content

```javascript
textContent
innerText
innerHTML
```

### Attributes

```javascript
getAttribute()
setAttribute()
hasAttribute()
removeAttribute()
```

### Classes

```javascript
classList.add()
classList.remove()
classList.toggle()
classList.contains()
classList.replace()
```

### Style

```javascript
element.style.color
element.style.backgroundColor
element.style.fontSize
```

### Create

```javascript
document.createElement()
```

### Insert

```javascript
append()
appendChild()
prepend()
before()
after()
```

### Remove

```javascript
remove()
removeChild()
```

### Traversal

```javascript
parentElement
children
firstElementChild
lastElementChild
nextElementSibling
previousElementSibling
closest()
```

### Data

```javascript
dataset
```

---

# 🧠 Quick Cheat Sheet

```javascript
// Select
document.getElementById("title");

document.querySelector(".box");

document.querySelectorAll(".item");

// Content
element.textContent = "Hello";

element.innerHTML = "<b>Hello</b>";

// Attribute
element.setAttribute("title", "Hello");

element.getAttribute("title");

element.removeAttribute("title");

// Class
element.classList.add("active");

element.classList.remove("active");

element.classList.toggle("active");

// Style
element.style.color = "red";

// Create
let div = document.createElement("div");

// Add
parent.append(div);

// Remove
div.remove();

// Parent
element.parentElement;

// Children
element.children;

// Data
element.dataset.id;
```

---

# 📝 Chapter 15 Practice Set

### Beginner

**1.** একটি `<h1>` element তৈরি করে JavaScript দিয়ে তার text পরিবর্তন করো।

**2.** একটি paragraph-এর color JavaScript দিয়ে পরিবর্তন করো।

**3.** একটি button-এর text পরিবর্তন করো।

**4.** একটি image-এর `src` এবং `alt` JavaScript দিয়ে পরিবর্তন করো।

---

### Intermediate

**5.** একটি input থেকে name নিয়ে `<h2>`-এ দেখাও।

**6.** JavaScript দিয়ে নতুন `<li>` তৈরি করে `<ul>`-এ add করো।

**7.** একটি button click করলে একটি CSS class toggle করো।

**8.** পাঁচটি paragraph select করে প্রত্যেকটির text পরিবর্তন করো।

---

### Advanced

**9.** একটি dynamic Todo List বানাও:

```text
Input
  ↓
Add Task
  ↓
Task List
  ↓
Delete Task
```

**10.** একটি Product Card JavaScript দিয়ে dynamically তৈরি করো:

```text
Product Image
Product Name
Price
Add to Cart Button
```

**11.** `data-id` ব্যবহার করে প্রতিটি product-এর ID store করো।

**12.** `closest()` ব্যবহার করে একটি card-এর parent element বের করো।

---

# 🎯 Chapter 15 Final Summary

আজ পর্যন্ত আমরা JavaScript-এর ভিতরের language concepts থেকে এসে **Browser-এর HTML-এর সাথে JavaScript-এর connection** শিখলাম।

সবচেয়ে গুরুত্বপূর্ণ flow:

```text
document
   ↓
Select Element
   ↓
Read / Modify
   ↓
Content
Attribute
Style
Class
   ↓
Create / Add / Remove
   ↓
Dynamic Webpage
```

আর DOM-এর সবচেয়ে গুরুত্বপূর্ণ ৫টি skill:

```text
1. Select
2. Read
3. Modify
4. Create
5. Remove
```

একজন **Frontend / Full Stack Developer** হিসেবে DOM খুব ভালোভাবে জানা অত্যন্ত গুরুত্বপূর্ণ, কারণ React শেখার আগেও বুঝতে হবে browser-এর HTML/UI আসলে কীভাবে কাজ করে।

**পরবর্তী: Chapter 16 — JavaScript Events & Event Handling**
এখানে আমরা `click`, `input`, `change`, `submit`, keyboard events, mouse events, event object, `addEventListener()`, event bubbling, capturing, delegation, `preventDefault()`, `stopPropagation()`—সব বিস্তারিত শিখব।
