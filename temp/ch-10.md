# 📘 JavaScript Complete Book — Chapter 10

# Objects — Object / Key-Value Data

JavaScript শেখার সময় **Objects** একটি অত্যন্ত গুরুত্বপূর্ণ topic।

তুমি যেহেতু পরে **React + Node.js + API + MongoDB** নিয়ে কাজ করবে, তাই Object ভালোভাবে বোঝা খুব জরুরি।

---

# 10.1 Object কী?

সহজভাবে:

> **Object হলো related data এবং functionality-কে key-value pair আকারে এক জায়গায় রাখার একটি structure।**

### Real-life Example

একজন Student-এর তথ্য:

```text
Name      → Shariar
Age       → 23
Department → CSE
University → DIU
```

JavaScript:

```javascript id="9k2h5s"
let student = {
    name: "Shariar",
    age: 23,
    department: "CSE",
    university: "DIU"
};
```

এখানে:

```text
name       → key
"Shariar"  → value

age        → key
23         → value
```

---

# 10.2 Object তৈরি করার Syntax

```javascript id="h9j7xw"
let user = {
    name: "Shariar",
    age: 23,
    city: "Dhaka"
};
```

Object-এর structure:

```text id="8h8y5p"
{
    key: value,
    key: value,
    key: value
}
```

---

# 10.3 Object Property

Object-এর ভিতরের key-value pair-কে সাধারণত **Property** বলা হয়।

```javascript id="pr3j7w"
let user = {
    name: "Shariar",
    age: 23
};
```

এখানে:

```text id="x2b7d0"
name → property
age  → property
```

---

# 10.4 Object Property Access

Object-এর property access করার দুটি প্রধান উপায় আছে:

1. Dot notation
2. Bracket notation

---

# 10.5 Dot Notation

```javascript id="j1j2z9"
let user = {
    name: "Shariar",
    age: 23
};

console.log(user.name);
console.log(user.age);
```

### Output

```text id="9n1q8d"
Shariar
23
```

Syntax:

```javascript id="g3i8dk"
object.property
```

---

# 10.6 Bracket Notation

```javascript id="w5h4p7"
let user = {
    name: "Shariar",
    age: 23
};

console.log(user["name"]);
console.log(user["age"]);
```

Output:

```text id="6i6m11"
Shariar
23
```

---

# 10.7 Dot vs Bracket

| Dot Notation                  | Bracket Notation                  |
| ----------------------------- | --------------------------------- |
| `user.name`                   | `user["name"]`                    |
| Simple                        | Dynamic access-এর জন্য useful     |
| Property name সরাসরি লিখতে হয় | String/expression ব্যবহার করা যায় |

---

# 10.8 Dynamic Property Access 🔥

ধরো:

```javascript id="ef6g0a"
let user = {
    name: "Shariar",
    age: 23
};

let property = "name";

console.log(user[property]);
```

### Output

```text id="r8g1se"
Shariar
```

এখানে:

```javascript id="04qg3x"
user[property]
```

মানে:

```javascript id="hmspw6"
user["name"]
```

---

# 10.9 কেন Dot Notation কাজ করবে না?

```javascript id="w3zj6u"
let property = "name";

console.log(user.property);
```

এটি খুঁজবে:

```text id="p7z9d1"
user["property"]
```

`name` নয়।

তাই dynamic property হলে:

```javascript id="6vby7g"
user[property]
```

ব্যবহার করতে হবে।

---

# 10.10 Property Add করা

Object তৈরি করার পর নতুন property যোগ করা যায়।

```javascript id="e0qf7s"
let user = {
    name: "Shariar"
};

user.age = 23;

console.log(user);
```

Output:

```text id="qud4jp"
{
    name: "Shariar",
    age: 23
}
```

---

# 10.11 Bracket দিয়ে Property Add

```javascript id="q4b2v4"
let user = {
    name: "Shariar"
};

user["city"] = "Dhaka";

console.log(user);
```

Output:

```text id="yq5e2j"
{
    name: "Shariar",
    city: "Dhaka"
}
```

---

# 10.12 Property Update

```javascript id="byb8ku"
let user = {
    name: "Shariar",
    age: 23
};

user.age = 24;

console.log(user.age);
```

Output:

```text id="5qv2sj"
24
```

---

# 10.13 Property Delete

```javascript id="3z9w5c"
let user = {
    name: "Shariar",
    age: 23,
    city: "Dhaka"
};

delete user.city;

console.log(user);
```

Output:

```text id="p6q2dn"
{
    name: "Shariar",
    age: 23
}
```

---

# 10.14 Property আছে কিনা Check

`in` operator:

```javascript id="6t2m7q"
let user = {
    name: "Shariar",
    age: 23
};

console.log("name" in user);
console.log("email" in user);
```

Output:

```text id="e1v9tr"
true
false
```

---

# 10.15 `hasOwnProperty()`

```javascript id="b4u5fz"
let user = {
    name: "Shariar",
    age: 23
};

console.log(user.hasOwnProperty("name"));
console.log(user.hasOwnProperty("email"));
```

Output:

```text id="3q8h5g"
true
false
```

Modern code-এ আরও robust approach হিসেবে `Object.hasOwn()`-ও আছে:

```javascript id="6ojj3e"
console.log(Object.hasOwn(user, "name"));
```

---

# 10.16 Object-এর মধ্যে বিভিন্ন Data Type

Object property-এর value যেকোনো valid JavaScript value হতে পারে।

```javascript id="5j0wqk"
let user = {
    name: "Shariar",
    age: 23,
    isStudent: true,
    skills: ["HTML", "CSS", "JavaScript"],
    address: null
};

console.log(user);
```

এখানে:

```text id="d2c1y0"
String
Number
Boolean
Array
null
```

সবই আছে।

---

# 10.17 Object-এর মধ্যে Object

এটাকে **Nested Object** বলে।

```javascript id="u7x5q2"
let user = {
    name: "Shariar",
    address: {
        city: "Dhaka",
        country: "Bangladesh"
    }
};

console.log(user.address.city);
```

Output:

```text id="v0t3fb"
Dhaka
```

---

# 10.18 Nested Object Access

```javascript id="b9x6r1"
let student = {
    name: "Shariar",
    university: {
        name: "Daffodil International University",
        department: "CSE"
    }
};

console.log(student.university.name);
console.log(student.university.department);
```

Output:

```text id="h2l3r4"
Daffodil International University
CSE
```

---

# 10.19 Object-এর মধ্যে Array

```javascript id="o1p8n3"
let user = {
    name: "Shariar",
    skills: ["HTML", "CSS", "JavaScript", "React"]
};

console.log(user.skills[0]);
console.log(user.skills[3]);
```

Output:

```text id="2x3z7c"
HTML
React
```

---

# 10.20 Array-এর মধ্যে Object 🔥

এটি real-world JavaScript-এ খুব common।

```javascript id="5p4r7n"
let users = [
    {
        name: "Shariar",
        age: 23
    },
    {
        name: "Rahim",
        age: 22
    }
];

console.log(users[0].name);
```

Output:

```text id="n7f2c4"
Shariar
```

API response-এ তুমি এই ধরনের structure অনেক দেখতে পাবে।

---

# 10.21 Object Method

Object-এর property-এর value যদি function হয়, সেটিকে সাধারণত **Method** বলা হয়।

```javascript id="q8x3m1"
let user = {
    name: "Shariar",

    sayHello: function () {
        console.log("Hello!");
    }
};

user.sayHello();
```

Output:

```text id="x6k9v2"
Hello!
```

---

# 10.22 Method-এর Short Syntax

Modern JavaScript:

```javascript id="h2w7p9"
let user = {
    name: "Shariar",

    sayHello() {
        console.log("Hello!");
    }
};

user.sayHello();
```

এটি cleaner syntax।

---

# 10.23 Object Method-এ `this` 🔥🔥

এখন খুব গুরুত্বপূর্ণ concept:

```javascript id="g7n2x4"
let user = {
    name: "Shariar",

    introduce() {
        console.log(`My name is ${this.name}`);
    }
};

user.introduce();
```

### Output

```text id="p4m8z1"
My name is Shariar
```

এখানে:

```javascript id="2k5r6v"
this.name
```

মানে:

> যে object method-টি call করেছে, সেই object's `name` property।

---

# 10.24 `this` সহজভাবে

```javascript id="r8j4w3"
let user = {
    name: "Ripon",

    showName() {
        console.log(this.name);
    }
};
```

যখন:

```javascript id="9q3t5b"
user.showName();
```

তখন:

```text id="2v8c1k"
this → user
```

তাই:

```javascript id="x4y7m2"
this.name
```

হয়:

```javascript id="f5k8n3"
user.name
```

---

# 10.25 `this` এবং Arrow Function

Object method-এ arrow function ব্যবহার করলে `this` সাধারণত object-কে refer করে না।

❌ এই pattern এড়িয়ে চলো:

```javascript id="e6w9q1"
let user = {
    name: "Shariar",

    sayHello: () => {
        console.log(this.name);
    }
};

user.sayHello();
```

এখানে `this` lexical context থেকে আসে।

Object method-এর জন্য সাধারণত:

```javascript id="q3m7v8"
sayHello() {
    console.log(this.name);
}
```

ব্যবহার করা সহজ ও সঠিক।

---

# 10.26 Object Property Shorthand

যদি variable এবং property-এর নাম একই হয়:

```javascript id="n2r5x7"
let name = "Shariar";
let age = 23;

let user = {
    name,
    age
};

console.log(user);
```

Output:

```text id="c6v9k2"
{
    name: "Shariar",
    age: 23
}
```

আগে লিখতে হতো:

```javascript id="a5s8d1"
let user = {
    name: name,
    age: age
};
```

Modern JavaScript-এ shorthand ব্যবহার করা যায়।

---

# 10.27 Computed Property Names

Property name dynamically তৈরি করা যায়।

```javascript id="f2g6h8"
let key = "username";

let user = {
    [key]: "Shariar"
};

console.log(user);
```

Output:

```text id="k7m1p4"
{
    username: "Shariar"
}
```

---

# 10.28 Object.keys()

Object-এর সব property name-এর Array:

```javascript id="x1v4z7"
let user = {
    name: "Shariar",
    age: 23,
    city: "Dhaka"
};

console.log(Object.keys(user));
```

Output:

```text id="c3n6b8"
["name", "age", "city"]
```

---

# 10.29 Object.values()

সব value:

```javascript id="w9r2k5"
console.log(Object.values(user));
```

Output:

```text id="j4m7q1"
["Shariar", 23, "Dhaka"]
```

---

# 10.30 Object.entries()

Key + value pair:

```javascript id="d8f1s4"
console.log(Object.entries(user));
```

Output:

```text id="a2h5k9"
[
    ["name", "Shariar"],
    ["age", 23],
    ["city", "Dhaka"]
]
```

---

# 10.31 Object-এর উপর Loop

### `for...in`

```javascript id="y7p3c6"
let user = {
    name: "Shariar",
    age: 23,
    city: "Dhaka"
};

for (let key in user) {
    console.log(key, user[key]);
}
```

Output:

```text id="z4m8n1"
name Shariar
age 23
city Dhaka
```

---

# 10.32 `Object.entries()` + `for...of`

```javascript id="s6k2v9"
for (let [key, value] of Object.entries(user)) {
    console.log(key, value);
}
```

Output:

```text id="p1q5r8"
name Shariar
age 23
city Dhaka
```

---

# 10.33 Object Destructuring 🔥

Object থেকে property সরাসরি variable-এ নেওয়া যায়।

```javascript id="u3w7x2"
let user = {
    name: "Shariar",
    age: 23
};

let { name, age } = user;

console.log(name);
console.log(age);
```

Output:

```text id="h8j4k6"
Shariar
23
```

---

# 10.34 Destructuring Rename

```javascript id="b5n9q2"
let user = {
    name: "Shariar",
    age: 23
};

let { name: userName, age: userAge } = user;

console.log(userName);
console.log(userAge);
```

Output:

```text id="m7c3x1"
Shariar
23
```

---

# 10.35 Destructuring Default Value

```javascript id="k2d6f9"
let user = {
    name: "Shariar"
};

let { name, age = 23 } = user;

console.log(name);
console.log(age);
```

Output:

```text id="r4t8v5"
Shariar
23
```

---

# 10.36 Nested Destructuring

```javascript id="n1m5q8"
let user = {
    name: "Shariar",
    address: {
        city: "Dhaka",
        country: "Bangladesh"
    }
};

let {
    address: { city, country }
} = user;

console.log(city);
console.log(country);
```

Output:

```text id="s3x7z9"
Dhaka
Bangladesh
```

---

# 10.37 Rest Property

Object-এর কিছু property আলাদা করে বাকি property নিতে:

```javascript id="v6k2p4"
let user = {
    name: "Shariar",
    age: 23,
    city: "Dhaka",
    country: "Bangladesh"
};

let { name, ...others } = user;

console.log(name);
console.log(others);
```

Output:

```text id="q8m1d5"
Shariar

{
    age: 23,
    city: "Dhaka",
    country: "Bangladesh"
}
```

---

# 10.38 Spread Operator — Object

Object copy:

```javascript id="c4n7x2"
let user = {
    name: "Shariar",
    age: 23
};

let copy = { ...user };

console.log(copy);
```

Output:

```text id="f9k3m6"
{
    name: "Shariar",
    age: 23
}
```

---

# 10.39 Object Combine

```javascript id="r5v8q1"
let personal = {
    name: "Shariar",
    age: 23
};

let professional = {
    role: "Web Developer",
    skill: "JavaScript"
};

let profile = {
    ...personal,
    ...professional
};

console.log(profile);
```

Output:

```text id="x2c6m9"
{
    name: "Shariar",
    age: 23,
    role: "Web Developer",
    skill: "JavaScript"
}
```

---

# 10.40 Spread-এর মাধ্যমে Property Override

```javascript id="k7p2v5"
let user = {
    name: "Shariar",
    age: 23
};

let updatedUser = {
    ...user,
    age: 24
};

console.log(updatedUser);
```

Output:

```text id="m3x8q1"
{
    name: "Shariar",
    age: 24
}
```

শেষের property আগের value override করে।

---

# 10.41 Object Reference 🔥

এটা Array-এর মতোই গুরুত্বপূর্ণ।

```javascript id="s9d4f7"
let user1 = {
    name: "Shariar"
};

let user2 = user1;

user2.name = "Ripon";

console.log(user1.name);
```

Output:

```text id="j6k1m8"
Ripon
```

কারণ দুটো variable একই object reference করছে।

---

# 10.42 Object Copy

```javascript id="p3v7x9"
let user1 = {
    name: "Shariar"
};

let user2 = { ...user1 };

user2.name = "Ripon";

console.log(user1.name);
console.log(user2.name);
```

Output:

```text id="q5m2c8"
Shariar
Ripon
```

---

# 10.43 Shallow Copy

Spread operator:

```javascript id="z8n4k1"
{ ...object }
```

একটি **shallow copy** তৈরি করে।

যদি nested object থাকে, তাহলে বিষয়টি একটু আলাদা।

```javascript id="h2v6p9"
let user1 = {
    name: "Shariar",
    address: {
        city: "Dhaka"
    }
};

let user2 = { ...user1 };

user2.address.city = "Chittagong";

console.log(user1.address.city);
```

Output:

```text id="b7m3x5"
Chittagong
```

কারণ nested `address` object-এর reference একই থাকতে পারে।

---

# 10.44 Deep Copy — Basic Approach

Simple JSON-compatible data-এর জন্য:

```javascript id="t4q8n2"
let user1 = {
    name: "Shariar",
    address: {
        city: "Dhaka"
    }
};

let user2 = JSON.parse(JSON.stringify(user1));

user2.address.city = "Chittagong";

console.log(user1.address.city);
console.log(user2.address.city);
```

Output:

```text id="v1k5c9"
Dhaka
Chittagong
```

তবে modern JavaScript-এ structured data-এর জন্য:

```javascript id="r6m2p8"
structuredClone()
```

আরও ভালো option হতে পারে।

```javascript id="x9c4d7"
let user2 = structuredClone(user1);
```

---

# 10.45 Optional Chaining `?.`

Nested property না থাকলে error এড়াতে:

```javascript id="n5v8k2"
let user = {
    name: "Shariar"
};

console.log(user.address?.city);
```

Output:

```text id="m7q3x1"
undefined
```

Without optional chaining:

```javascript id="f2d6p9"
console.log(user.address.city);
```

এতে error হতে পারে কারণ `address` undefined।

---

# 10.46 Nullish Coalescing + Object

```javascript id="j8m4v6"
let user = {
    name: "Shariar"
};

let city = user.address?.city ?? "Unknown";

console.log(city);
```

Output:

```text id="c1x7k5"
Unknown
```

---

# 10.47 Object.freeze()

Object-এর property পরিবর্তন বন্ধ করতে:

```javascript id="q9v3m2"
let user = {
    name: "Shariar",
    age: 23
};

Object.freeze(user);

user.age = 24;

console.log(user.age);
```

Output সাধারণত:

```text id="w5k8n1"
23
```

Strict mode-এ illegal modification error-ও হতে পারে।

---

# 10.48 `Object.isFrozen()`

```javascript id="a6p2r9"
console.log(Object.isFrozen(user));
```

Output:

```text id="h3x7m4"
true
```

---

# 10.49 Object.seal()

`seal()` করলে:

* নতুন property add করা যায় না
* property delete করা যায় না
* existing writable property পরিবর্তন করা যায়

```javascript id="d8k1v5"
let user = {
    name: "Shariar",
    age: 23
};

Object.seal(user);

user.age = 24;
delete user.name;

console.log(user);
```

Output:

```text id="p4m9x2"
{
    name: "Shariar",
    age: 24
}
```

---

# 10.50 `Object.isSealed()`

```javascript id="n7q3c8"
console.log(Object.isSealed(user));
```

Output:

```text id="z2v6k1"
true
```

---

# 10.51 Object.assign()

একটি Object-এর properties অন্য Object-এ copy করা যায়।

```javascript id="u4m8p2"
let user = {
    name: "Shariar"
};

let extra = {
    age: 23
};

Object.assign(user, extra);

console.log(user);
```

Output:

```text id="x6k1r9"
{
    name: "Shariar",
    age: 23
}
```

Spread operator-এর কারণে modern JavaScript-এ অনেক ক্ষেত্রে:

```javascript id="c5v9n3"
let user = {
    ...user,
    ...extra
};
```

ধরনের approach বেশি দেখা যায়।

---

# 10.52 Object.create()

Prototype-based object তৈরি করতে:

```javascript id="j2q7m4"
let person = {
    greet() {
        console.log("Hello");
    }
};

let user = Object.create(person);

user.greet();
```

Output:

```text id="r8x3k6"
Hello
```

এটি পরে **Prototypes** chapter-এ বিস্তারিত শিখব।

---

# 10.53 Object Constructor

`new Object()` দিয়ে Object তৈরি করা যায়:

```javascript id="v5n9p2"
let user = new Object();

user.name = "Shariar";
user.age = 23;

console.log(user);
```

তবে সাধারণ object literal:

```javascript id="q1m6x8"
let user = {
    name: "Shariar",
    age: 23
};
```

বেশি clean এবং common।

---

# 10.54 Object-এর Property Name হিসেবে Number

```javascript id="b7k2d5"
let data = {
    1: "One",
    2: "Two"
};

console.log(data[1]);
```

Output:

```text id="z4x8m1"
One
```

Object keys internally সাধারণত String বা Symbol হিসেবে handled হয়।

---

# 10.55 Property Name-এ Space

```javascript id="s3q9v6"
let user = {
    "full name": "Shariar Ahamed"
};

console.log(user["full name"]);
```

এখানে:

```javascript id="n1c5k7"
user.full name
```

লেখা যাবে না।

তাই bracket notation ব্যবহার করতে হবে।

---

# 10.56 Object-এর মধ্যে Function

```javascript id="x8m2p4"
let calculator = {

    add(a, b) {
        return a + b;
    },

    subtract(a, b) {
        return a - b;
    }
};

console.log(calculator.add(10, 5));
console.log(calculator.subtract(10, 5));
```

Output:

```text id="q6v9n1"
15
5
```

---

# 10.57 Real-life Example — User Object

```javascript id="c2k7m5"
let user = {
    id: 101,
    name: "Shariar Ahamed Ripon",
    email: "ripon@example.com",
    age: 23,
    isStudent: true,
    skills: [
        "HTML",
        "CSS",
        "JavaScript",
        "React"
    ],
    address: {
        city: "Dhaka",
        country: "Bangladesh"
    },

    introduce() {
        console.log(`Hi, I am ${this.name}`);
    }
};

console.log(user.name);
console.log(user.skills[2]);
console.log(user.address.city);

user.introduce();
```

### Output

```text id="g8x3r2"
Shariar Ahamed Ripon
JavaScript
Dhaka
Hi, I am Shariar Ahamed Ripon
```

এখানে একসাথে ব্যবহার হয়েছে:

```text id="v4n6k1"
Object
Array
Nested Object
Method
this
Template Literal
```

---

# 10.58 API Response-এর মতো Object

Web development-এ তুমি প্রায়ই এমন data পাবে:

```javascript id="m5q8x2"
let response = {
    success: true,
    message: "User fetched successfully",
    data: {
        id: 101,
        name: "Shariar",
        email: "shariar@example.com"
    }
};

console.log(response.data.name);
```

Output:

```text id="z7c3v9"
Shariar
```

React/Node.js-এ এই ধরনের nested object প্রচুর ব্যবহার হবে।

---

# 10.59 Object + Array + `map()`

```javascript id="k2m6p8"
let users = [
    {
        name: "Shariar",
        age: 23
    },
    {
        name: "Rahim",
        age: 22
    },
    {
        name: "Karim",
        age: 24
    }
];

let names = users.map(user => user.name);

console.log(names);
```

Output:

```text id="q4x8n1"
["Shariar", "Rahim", "Karim"]
```

এটি real-world frontend development-এর খুব common pattern।

---

# 10.60 Object + `filter()`

```javascript id="r6k2v5"
let users = [
    { name: "Shariar", age: 23 },
    { name: "Rahim", age: 17 },
    { name: "Karim", age: 25 }
];

let adults = users.filter(user => user.age >= 18);

console.log(adults);
```

Output:

```text id="x9m3c7"
[
    { name: "Shariar", age: 23 },
    { name: "Karim", age: 25 }
]
```

---

# 10.61 Object + `find()`

```javascript id="p3v7k1"
let users = [
    { id: 1, name: "Shariar" },
    { id: 2, name: "Rahim" },
    { id: 3, name: "Karim" }
];

let user = users.find(user => user.id === 2);

console.log(user);
```

Output:

```text id="m8q2x5"
{
    id: 2,
    name: "Rahim"
}
```

---

# 10.62 Object + `reduce()`

ধরো product list:

```javascript id="f6k9r2"
let products = [
    { name: "Laptop", price: 70000 },
    { name: "Mouse", price: 1000 },
    { name: "Keyboard", price: 3000 }
];

let total = products.reduce((sum, product) => {
    return sum + product.price;
}, 0);

console.log(total);
```

Output:

```text id="c1v5n8"
74000
```

---

# 10.63 `Object.keys()` + `map()`

```javascript id="w4m7p2"
let user = {
    name: "Shariar",
    age: 23,
    city: "Dhaka"
};

let keys = Object.keys(user);

let upperKeys = keys.map(key => key.toUpperCase());

console.log(upperKeys);
```

Output:

```text id="z6x3k9"
["NAME", "AGE", "CITY"]
```

---

# 10.64 Object-এর গুরুত্বপূর্ণ Static Methods

| Method                 | কাজ                    |
| ---------------------- | ---------------------- |
| `Object.keys()`        | সব keys                |
| `Object.values()`      | সব values              |
| `Object.entries()`     | key-value pairs        |
| `Object.assign()`      | Object copy/merge      |
| `Object.freeze()`      | modification বন্ধ      |
| `Object.seal()`        | add/delete বন্ধ        |
| `Object.isFrozen()`    | frozen কিনা            |
| `Object.isSealed()`    | sealed কিনা            |
| `Object.hasOwn()`      | own property check     |
| `Object.create()`      | prototype-based object |
| `Object.fromEntries()` | entries → Object       |

---

# 10.65 `Object.fromEntries()`

`Object.entries()`-এর বিপরীত ধারণা।

```javascript id="j5r8x2"
let entries = [
    ["name", "Shariar"],
    ["age", 23]
];

let user = Object.fromEntries(entries);

console.log(user);
```

Output:

```text id="n2m6q9"
{
    name: "Shariar",
    age: 23
}
```

---

# 10.66 Object-এর Key Order

Modern JavaScript-এ property ordering-এর defined rules আছে, তবে সাধারণ application code-এ Object-কে ordered collection হিসেবে rely না করে data structure অনুযায়ী Array/Map ব্যবহার করা ভালো।

যদি order গুরুত্বপূর্ণ হয়, বিশেষ করে arbitrary key-value collection-এর ক্ষেত্রে `Map` অনেক ক্ষেত্রে বেশি appropriate।

`Map` আমরা পরের collection-related chapter-এ বিস্তারিত দেখব।

---

# 10.67 Object vs Map — Basic Idea

| Object                     | Map                          |
| -------------------------- | ---------------------------- |
| সাধারণ data/entity-এর জন্য | Key-value collection-এর জন্য |
| String/Symbol keys         | যেকোনো value key হতে পারে    |
| `user.name`                | `map.get(key)`               |
| JSON-এর সাথে natural       | Iteration-friendly           |

এখন শুধু ধারণাটা রাখো; পরে বিস্তারিত আসবে।

---

# 10.68 Object-এর সবচেয়ে গুরুত্বপূর্ণ Pattern

Real-world API data:

```javascript id="u7m2x5"
const user = {
    id: 101,
    profile: {
        name: "Shariar",
        contact: {
            email: "shariar@example.com"
        }
    }
};
```

Access:

```javascript id="k4p8n1"
console.log(user.profile.name);
console.log(user.profile.contact.email);
```

Optional chaining:

```javascript id="m6q3v9"
console.log(user.profile?.contact?.phone);
```

এটি undefined property-এর ক্ষেত্রে application crash হওয়ার ঝুঁকি কমাতে useful।

---

# 🧠 Chapter 10 — Object Mental Model

একটি Object এভাবে চিন্তা করো:

```text id="r2x7m5"
                 OBJECT
                    │
       ┌────────────┼────────────┐
       ↓            ↓            ↓
     Property     Property      Method
       │            │             │
      name         age          greet()
       ↓            ↓             ↓
   "Shariar"       23          function
```

আর nested হলে:

```text id="c8n4k1"
user
 │
 ├── name
 ├── age
 ├── skills[]
 │
 └── address
      ├── city
      └── country
```

---

# 🔥 Chapter 10 — Must Know

এইগুলো অবশ্যই ভালোভাবে আয়ত্ত করবে:

```text
1. Object কী
2. Property
3. Key / Value
4. Dot notation
5. Bracket notation
6. Dynamic property access
7. Add property
8. Update property
9. Delete property
10. in operator
11. Object.hasOwn()
12. Nested Object
13. Array inside Object
14. Object inside Array
15. Methods
16. this
17. Property shorthand
18. Computed properties
19. Object.keys()
20. Object.values()
21. Object.entries()
22. Object destructuring
23. Nested destructuring
24. Rest property
25. Spread operator
26. Object reference
27. Shallow copy
28. structuredClone()
29. Optional chaining
30. Nullish coalescing
31. Object.freeze()
32. Object.seal()
33. Object.assign()
34. Object.fromEntries()
35. Array of Objects
36. Object + map()
37. Object + filter()
38. Object + find()
39. Object + reduce()
```

---

# 📝 Practice Set — Chapter 10

## Practice 1 — Basic Object

এই Object তৈরি করো:

```text
Name: Shariar
Age: 23
Department: CSE
University: DIU
```

তারপর প্রতিটি property console-এ দেখাও।

---

## Practice 2 — Update

```javascript id="e7m3x8"
let user = {
    name: "Shariar",
    age: 23
};
```

`age` → `24` করো।

---

## Practice 3 — Add

একই Object-এ:

```text
city: Dhaka
```

যোগ করো।

---

## Practice 4 — Delete

`city` property delete করো।

---

## Practice 5 — Nested Object

এই structure তৈরি করো:

```text
student
 ├── name
 ├── age
 └── address
      ├── city
      └── country
```

তারপর `city` console-এ দেখাও।

---

## Practice 6 — Array of Objects

```javascript id="c5k8n2"
let students = [
    { name: "Shariar", marks: 90 },
    { name: "Rahim", marks: 75 },
    { name: "Karim", marks: 85 }
];
```

শুধু student-এর names বের করো।

Expected:

```text
["Shariar", "Rahim", "Karim"]
```

---

## Practice 7 — Filter

উপরের data থেকে যাদের marks `80` বা তার বেশি তাদের বের করো।

Expected:

```text
Shariar
Karim
```

---

## Practice 8 — Find

`marks` `80`-এর বেশি এমন প্রথম student বের করো।

---

## Practice 9 — Destructuring

```javascript id="m7q2x5"
let user = {
    name: "Shariar",
    age: 23,
    city: "Dhaka"
};
```

Destructuring করে তিনটি variable তৈরি করো:

```text
name
age
city
```

---

## Practice 10 — Mini Project 👨‍💻

```javascript id="p8v3k6"
let products = [
    {
        name: "Laptop",
        price: 70000,
        category: "Computer"
    },
    {
        name: "Mouse",
        price: 1000,
        category: "Accessories"
    },
    {
        name: "Keyboard",
        price: 3000,
        category: "Accessories"
    },
    {
        name: "Monitor",
        price: 15000,
        category: "Computer"
    }
];
```

এমন code লেখো যা:

1. সব product-এর নাম দেখাবে
2. সব product-এর total price বের করবে
3. `5000`-এর বেশি price-এর product বের করবে
4. `"Computer"` category-এর product বের করবে
5. সবচেয়ে expensive product বের করবে
6. শুধু product names-এর নতুন Array তৈরি করবে

---

# 🎯 Chapter 10 Final Summary

### Object-এর basic structure:

```javascript id="x4m8q1"
let user = {
    name: "Shariar",
    age: 23
};
```

Access:

```javascript id="p7c2v5"
user.name;
user["name"];
```

Update:

```javascript id="n3k9x6"
user.age = 24;
```

Add:

```javascript id="r5m1q8"
user.city = "Dhaka";
```

Delete:

```javascript id="v2d7p4"
delete user.city;
```

Methods:

```javascript id="j8q3n6"
user.sayHello();
```

`this`:

```javascript id="s4x9k2"
this.name;
```

Keys/values:

```javascript id="m6p1v8"
Object.keys(user);
Object.values(user);
Object.entries(user);
```

Destructuring:

```javascript id="c9r4x7"
const { name, age } = user;
```

Spread:

```javascript id="h2k5m9"
const copy = { ...user };
```

Optional chaining:

```javascript id="q7v3n1"
user.address?.city;
```

---

## ⭐ Chapter 10-এর সবচেয়ে গুরুত্বপূর্ণ ১০টি

```text
1. Object = key-value structure
2. Dot notation
3. Bracket notation
4. Nested Object
5. Array of Objects
6. Object Methods
7. this
8. Object Destructuring
9. Object.keys / values / entries
10. Spread + Reference + Shallow Copy
```

**Chapter 9-এর Array + Chapter 10-এর Object**—এই দুইটা একসাথে ভালোভাবে বুঝে ফেললে JavaScript-এর real-world data handling অনেক পরিষ্কার হয়ে যাবে। পরের অধ্যায়ে আমরা **Advanced Objects, Prototypes & Prototype Chain**-এর দিকে যাব, যেখানে JavaScript-এর ভিতরের object system কীভাবে কাজ করে সেটা বুঝব।
