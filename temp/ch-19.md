# 📘 Chapter 19 — JSON (JavaScript Object Notation)

### JSON — Data আদান-প্রদান ও API Communication-এর সবচেয়ে গুরুত্বপূর্ণ Format

JavaScript-এর Chapter 18-এ আমরা `localStorage`, `sessionStorage`, Cookies এবং browser storage শিখেছি। সেখানে একটা জিনিস বারবার ব্যবহার করেছি:

```javascript
JSON.stringify();
JSON.parse();
```

এখন Chapter 19-এ আমরা **JSON কী, কেন দরকার, কীভাবে কাজ করে, JSON ↔ JavaScript conversion, nested JSON, arrays, API response, JSON file, validation, limitations এবং real-world use**—সবকিছু শিখব।

---

# 19.1 JSON কী?

**JSON = JavaScript Object Notation**

JSON হলো এমন একটি **text-based data format**, যেটা বিভিন্ন application-এর মধ্যে data আদান-প্রদানের জন্য ব্যাপকভাবে ব্যবহৃত হয়।

সহজভাবে:

> **JSON হলো structured data লেখার একটি standard format।**

Example:

```json
{
  "name": "Shariar",
  "age": 24,
  "role": "Developer"
}
```

এটি দেখতে JavaScript object-এর মতো।

কিন্তু মনে রাখবে:

> **JSON নিজে JavaScript Object নয়। JSON হলো text format।**

---

# 19.2 Real-life Example

ধরো Frontend থেকে Backend-এ user data পাঠাবে।

Frontend:

```text
Name: Shariar
Email: shariar@gmail.com
Age: 24
```

Backend-এ structured data পাঠানো যায়:

```json
{
  "name": "Shariar",
  "email": "shariar@gmail.com",
  "age": 24
}
```

Backend data process করে database-এ save করতে পারে।

---

# 19.3 কেন JSON এত গুরুত্বপূর্ণ?

Modern web development-এ:

```text
Frontend
   ↓
HTTP Request
   ↓
API
   ↓
Backend
   ↓
Database
```

এখানে data আদান-প্রদানের জন্য JSON খুব common।

যেমন:

```text
React
   ↕
REST API
   ↕
Node.js / Express
   ↕
Database
```

তুমি Full Stack Developer হিসেবে JSON প্রায় প্রতিদিনই ব্যবহার করবে।

---

# 19.4 JSON-এর Basic Structure

```json
{
  "name": "Shariar",
  "age": 24,
  "student": true
}
```

এখানে:

```text
"name"    → Key
"Shariar" → Value

"age"     → Key
24        → Value
```

অর্থাৎ:

```text
Key : Value
```

---

# 19.5 JSON Data Types

JSON-এ মূলত ৬ ধরনের data থাকে:

```text
String
Number
Boolean
Object
Array
null
```

---

# 19.6 JSON String

```json
{
  "name": "Shariar"
}
```

String অবশ্যই double quote-এর মধ্যে হবে।

✅ Correct:

```json
{
  "name": "Shariar"
}
```

❌ Invalid JSON:

```json
{
  "name": "Shariar"
}
```

JSON standard-এ string-এর জন্য **double quotes** ব্যবহার করতে হয়।

---

# 19.7 JSON Number

```json
{
  "age": 24,
  "price": 70000,
  "rating": 4.5
}
```

Number-এর জন্য quote লাগে না।

---

# 19.8 JSON Boolean

```json
{
  "isStudent": true,
  "isActive": false
}
```

এখানে:

```text
true
false
```

quote-এর মধ্যে থাকবে না।

---

# 19.9 JSON `null`

```json
{
  "middleName": null
}
```

এর অর্থ এখানে বর্তমানে কোনো value নেই।

---

# 19.10 JSON Array

```json
{
  "skills": ["HTML", "CSS", "JavaScript", "React"]
}
```

Array-এর মধ্যে multiple values রাখা যায়।

---

# 19.11 JSON Object

Nested object:

```json
{
  "name": "Shariar",
  "address": {
    "city": "Dhaka",
    "country": "Bangladesh"
  }
}
```

এখানে `address` নিজেই একটি object।

---

# 19.12 Nested JSON

Real-world API response-এ nested data খুব common।

```json
{
  "user": {
    "id": 101,
    "name": "Shariar",
    "profile": {
      "role": "Developer",
      "skills": ["JavaScript", "React", "Node.js"]
    }
  }
}
```

JavaScript-এ:

```javascript
console.log(data.user.name);
```

Output:

```text
Shariar
```

আর:

```javascript
console.log(data.user.profile.skills[0]);
```

Output:

```text
JavaScript
```

---

# 19.13 JSON বনাম JavaScript Object

JavaScript object:

```javascript
const user = {
  name: "Shariar",
  age: 24,
};
```

JSON:

```json
{
  "name": "Shariar",
  "age": 24
}
```

দেখতে একই রকম হলেও পার্থক্য আছে।

| JavaScript Object                          | JSON                         |
| ------------------------------------------ | ---------------------------- |
| JavaScript-এর data structure               | Text-based data format       |
| Key quote ছাড়া হতে পারে                    | Key double quote-এ থাকতে হবে |
| Single quote string হিসেবে ব্যবহার করা যায় | String-এর জন্য double quote  |
| `undefined` থাকতে পারে                     | `undefined` নেই              |
| Function থাকতে পারে                        | Function নেই                 |
| `Date` object থাকতে পারে                   | Native Date type নেই         |
| Methods থাকতে পারে                         | Methods নেই                  |
| Directly executable data structure         | Data representation          |

---

# 19.14 JSON কেন JavaScript Object নয়?

ধরো:

```javascript
const user = {
  name: "Shariar",
};

console.log(typeof user);
```

Output:

```text
object
```

এখন:

```javascript
const jsonData = '{"name":"Shariar"}';

console.log(typeof jsonData);
```

Output:

```text
string
```

অর্থাৎ JSON representation সাধারণত JavaScript-এর কাছে **string** হিসেবে থাকে।

---

# 19.15 `JSON.stringify()`

JavaScript value → JSON string

এটাই `JSON.stringify()`-এর কাজ।

```javascript
const user = {
  name: "Shariar",
  age: 24,
};

const jsonData = JSON.stringify(user);

console.log(jsonData);
```

Output:

```text
{"name":"Shariar","age":24}
```

---

# 19.16 `typeof JSON.stringify()`

```javascript
const user = {
  name: "Shariar",
  age: 24,
};

const jsonData = JSON.stringify(user);

console.log(typeof jsonData);
```

Output:

```text
string
```

---

# 19.17 `JSON.parse()`

JSON string → JavaScript value

```javascript
const jsonData = '{"name":"Shariar","age":24}';

const user = JSON.parse(jsonData);

console.log(user);
```

Output:

```javascript
{
    name: "Shariar",
    age: 24
}
```

---

# 19.18 `JSON.parse()` Type

```javascript
console.log(typeof user);
```

Output:

```text
object
```

---

# 19.19 Main JSON Formula

এই দুইটা খুব ভালোভাবে মনে রাখবে:

### JavaScript → JSON

```javascript
JSON.stringify(data);
```

### JSON → JavaScript

```javascript
JSON.parse(json);
```

Mental model:

```text
JavaScript Object
       ↓
JSON.stringify()
       ↓
JSON String
```

Reverse:

```text
JSON String
     ↓
JSON.parse()
     ↓
JavaScript Object
```

---

# 19.20 Array → JSON

```javascript
const skills = ["HTML", "CSS", "JavaScript"];

const jsonSkills = JSON.stringify(skills);

console.log(jsonSkills);
```

Output:

```text
["HTML","CSS","JavaScript"]
```

---

# 19.21 JSON → Array

```javascript
const jsonSkills = '["HTML","CSS","JavaScript"]';

const skills = JSON.parse(jsonSkills);

console.log(skills);
```

Output:

```text
["HTML", "CSS", "JavaScript"]
```

---

# 19.22 Nested Object → JSON

```javascript
const student = {
  name: "Shariar",

  academic: {
    department: "CSE",
    semester: 8,
  },

  skills: ["JavaScript", "React", "Node.js"],
};

const jsonStudent = JSON.stringify(student);

console.log(jsonStudent);
```

Output:

```text
{"name":"Shariar","academic":{"department":"CSE","semester":8},"skills":["JavaScript","React","Node.js"]}
```

---

# 19.23 JSON Pretty Print

Normally JSON এক লাইনে আসে:

```javascript
JSON.stringify(student);
```

কিন্তু readable format চাইলে:

```javascript
JSON.stringify(student, null, 2);
```

Output:

```json
{
  "name": "Shariar",
  "academic": {
    "department": "CSE",
    "semester": 8
  },
  "skills": ["JavaScript", "React", "Node.js"]
}
```

এখানে:

```text
2 → indentation
```

দিচ্ছে।

---

# 19.24 `JSON.stringify()` এর তিনটি Argument

Syntax:

```javascript
JSON.stringify(value, replacer, space);
```

Example:

```javascript
JSON.stringify(user, null, 2);
```

এখানে:

```text
value    → user
replacer → null
space    → 2
```

---

# 19.25 `replacer`

`replacer` ব্যবহার করে কোন property JSON-এ থাকবে তা control করা যায়।

```javascript
const user = {
  name: "Shariar",
  age: 24,
  password: "secret123",
};

const json = JSON.stringify(user, ["name", "age"]);

console.log(json);
```

Output:

```json
{ "name": "Shariar", "age": 24 }
```

`password` বাদ গেছে।

⚠️ Sensitive data JSON-এ stringify না করাই ভালো যদি তার প্রয়োজন না থাকে।

---

# 19.26 Replacer Function

আরও advanced:

```javascript
const user = {
  name: "Shariar",
  age: 24,
  password: "secret",
};

const json = JSON.stringify(user, function (key, value) {
  if (key === "password") {
    return undefined;
  }

  return value;
});

console.log(json);
```

Output:

```json
{ "name": "Shariar", "age": 24 }
```

---

# 19.27 `JSON.parse()` Reviver

`JSON.parse()`-এরও second argument আছে:

```javascript
JSON.parse(text, reviver);
```

Example:

```javascript
const json = `
{
    "name": "Shariar",
    "age": 24
}
`;

const user = JSON.parse(json, function (key, value) {
  if (key === "age") {
    return value + 1;
  }

  return value;
});

console.log(user);
```

Output:

```javascript
{
    name: "Shariar",
    age: 25
}
```

---

# 19.28 JSON এ Function রাখা যায়?

না।

ধরো:

```javascript
const user = {
  name: "Shariar",

  greet() {
    console.log("Hello");
  },
};

const json = JSON.stringify(user);

console.log(json);
```

Output:

```json
{ "name": "Shariar" }
```

`greet()` বাদ গেছে।

কারণ:

> **JSON data format; executable function রাখার format নয়।**

---

# 19.29 `undefined` এবং JSON

```javascript
const user = {
  name: "Shariar",
  age: undefined,
};

console.log(JSON.stringify(user));
```

Output:

```json
{ "name": "Shariar" }
```

Object-এর property হিসেবে `undefined` সাধারণত বাদ যায়।

---

# 19.30 Array-এর মধ্যে `undefined`

```javascript
const data = ["HTML", undefined, "JavaScript"];

console.log(JSON.stringify(data));
```

Output:

```json
["HTML", null, "JavaScript"]
```

অর্থাৎ array-এর ক্ষেত্রে `undefined` → `null` হয়ে যায়।

---

# 19.31 `NaN` এবং Infinity

```javascript
const data = {
  a: NaN,
  b: Infinity,
  c: -Infinity,
};

console.log(JSON.stringify(data));
```

Output:

```json
{
  "a": null,
  "b": null,
  "c": null
}
```

JSON-এ এগুলোর আলাদা standard number representation নেই।

---

# 19.32 Date এবং JSON

JavaScript:

```javascript
const data = {
  createdAt: new Date(),
};

console.log(JSON.stringify(data));
```

Date সাধারণত ISO string representation-এ serialize হয়।

উদাহরণ:

```json
{
  "createdAt": "2026-09-16T09:55:00.000Z"
}
```

অর্থাৎ JSON-এ native `Date` type নেই।

---

# 19.33 JSON Parse করার সময় Date

JSON থেকে date parse করলে:

```javascript
const data = JSON.parse(json);

console.log(typeof data.createdAt);
```

Output:

```text
string
```

এটি আবার Date object বানাতে:

```javascript
const date = new Date(data.createdAt);

console.log(date);
```

---

# 19.34 Circular Reference

একটি object নিজেকেই reference করলে:

```javascript
const user = {
  name: "Shariar",
};

user.self = user;
```

এখন:

```javascript
JSON.stringify(user);
```

Error হবে:

```text
TypeError:
Converting circular structure to JSON
```

কারণ JSON-এর normal object structure circular reference represent করতে পারে না।

---

# 19.35 JSON Parse Error

Invalid JSON parse করলে error হবে।

```javascript
const json = '{"name":"Shariar"';

try {
  const data = JSON.parse(json);

  console.log(data);
} catch (error) {
  console.error("Invalid JSON:", error.message);
}
```

Output:

```text
Invalid JSON: ...
```

এখানে Chapter 14-এর `try...catch` আবার কাজে লাগছে।

---

# 19.36 JSON-এর Common Syntax Rules

### Rule 1 — Keys double quotes

✅

```json
{
  "name": "Shariar"
}
```

❌

```json
{
  "name": "Shariar"
}
```

---

### Rule 2 — String double quotes

✅

```json
{
  "name": "Shariar"
}
```

❌

```json
{
  "name": "Shariar"
}
```

---

### Rule 3 — Trailing comma নয়

❌

```json
{
  "name": "Shariar",
  "age": 24
}
```

✅

```json
{
  "name": "Shariar",
  "age": 24
}
```

---

### Rule 4 — Comments নেই

❌

```json
{
  "name": "Shariar" // user name
}
```

Standard JSON-এ comments নেই।

---

# 19.37 JSON File

JSON আলাদা `.json` file হিসেবেও রাখা যায়।

File:

```text
students.json
```

Content:

```json
{
  "students": [
    {
      "id": 1,
      "name": "Shariar",
      "department": "CSE"
    },
    {
      "id": 2,
      "name": "Rahim",
      "department": "EEE"
    }
  ]
}
```

এ ধরনের file configuration বা static data-এর জন্য ব্যবহার করা যায়।

---

# 19.38 `fetch()` দিয়ে JSON File পড়া

এখানে Chapter 21-এ আমরা `fetch()` বিস্তারিত শিখব; এখন শুধু JSON-এর সাথে relation দেখছি।

```javascript
fetch("students.json")
  .then((response) => response.json())
  .then((data) => {
    console.log(data);
  });
```

Output:

```javascript
{
    students: [
        {
            id: 1,
            name: "Shariar",
            department: "CSE"
        },
        ...
    ]
}
```

---

# 19.39 API Response হিসেবে JSON

ধরো server response:

```json
{
  "success": true,
  "message": "Login successful",
  "user": {
    "id": 101,
    "name": "Shariar",
    "role": "student"
  }
}
```

Frontend:

```javascript
console.log(data.success);
```

Output:

```text
true
```

আর:

```javascript
console.log(data.user.name);
```

Output:

```text
Shariar
```

---

# 19.40 REST API-তে JSON

একটি typical API request:

```text
POST /api/users
```

Request body:

```json
{
  "name": "Shariar",
  "email": "shariar@gmail.com",
  "role": "student"
}
```

Server response:

```json
{
  "success": true,
  "message": "User created",
  "userId": 101
}
```

এই pattern তুমি React + Node.js/Express project-এ অনেক দেখবে।

---

# 19.41 JSON Headers

HTTP API-তে JSON পাঠানোর সময় সাধারণত:

```http
Content-Type: application/json
```

ব্যবহার করা হয়।

JavaScript:

```javascript
fetch("/api/users", {
  method: "POST",

  headers: {
    "Content-Type": "application/json",
  },

  body: JSON.stringify({
    name: "Shariar",
    email: "shariar@gmail.com",
  }),
});
```

এখানে:

```javascript
body: JSON.stringify(...)
```

খুব গুরুত্বপূর্ণ।

---

# 19.42 কেন `JSON.stringify()` দরকার?

এই object:

```javascript
{
    name: "Shariar",
    age: 24
}
```

JavaScript object।

HTTP request body-তে JSON পাঠাতে:

```javascript
JSON.stringify({
  name: "Shariar",
  age: 24,
});
```

হলে:

```json
{ "name": "Shariar", "age": 24 }
```

হয়।

---

# 19.43 Server থেকে JSON Response

Server response যদি JSON হয়, তখন:

```javascript
const response = await fetch("/api/users");

const data = await response.json();
```

এখানে:

```javascript
response.json();
```

response body-এর JSON data parse করে JavaScript value দেয়।

এটি Chapter 21-এ বিস্তারিত শিখব।

---

# 19.44 localStorage + JSON

Chapter 18-এর concept আবার:

```javascript
const user = {
  name: "Shariar",
  role: "Developer",
};

localStorage.setItem("user", JSON.stringify(user));
```

Read:

```javascript
const user = JSON.parse(localStorage.getItem("user"));
```

এখানে JSON হলো bridge:

```text
JavaScript Object
       ↓
JSON.stringify()
       ↓
String
       ↓
localStorage
```

---

# 19.45 JSON Deep Copy Pattern

Primitive/simple JSON-compatible data-এর ক্ষেত্রে:

```javascript
const original = {
  name: "Shariar",
  skills: ["HTML", "CSS"],
};

const copy = JSON.parse(JSON.stringify(original));
```

এতে আলাদা object তৈরি হয়।

```javascript
copy.name = "Ripon";

console.log(original.name);
```

Output:

```text
Shariar
```

তবে এটি **general-purpose deep clone solution নয়**।

কারণ `Date`, `Map`, `Set`, `undefined`, functions, circular references ইত্যাদির ক্ষেত্রে JSON cloning data পরিবর্তন/হারাতে পারে বা error দিতে পারে।

Modern JavaScript-এ সাধারণ structured data clone করতে:

```javascript
structuredClone(original);
```

অনেক ক্ষেত্রে বেশি উপযুক্ত।

---

# 19.46 JSON Security

JSON নিজে dangerous নয়।

কিন্তু **untrusted JSON data কীভাবে ব্যবহার করছো**, সেটাই গুরুত্বপূর্ণ।

ধরো server থেকে:

```json
{
  "message": "<img src=x onerror=alert('XSS')>"
}
```

তুমি যদি এটাকে:

```javascript
element.innerHTML = data.message;
```

দাও, security problem তৈরি হতে পারে।

বরং সাধারণ text হলে:

```javascript
element.textContent = data.message;
```

ব্যবহার করা safer।

---

# 19.47 JSON Schema-এর Basic Idea

Large project-এ JSON data-এর structure define করার প্রয়োজন হয়।

যেমন:

```text
User
 ├── id → number
 ├── name → string
 ├── email → string
 └── age → number
```

JSON Schema দিয়ে বলা যায়:

```json
{
  "type": "object",
  "properties": {
    "name": {
      "type": "string"
    },
    "age": {
      "type": "number"
    }
  },
  "required": ["name", "age"]
}
```

এটি data structure validation-এর জন্য ব্যবহার করা যায়।

---

# 19.48 Real-life Student API

ধরো API response:

```json
{
  "success": true,
  "students": [
    {
      "id": 1,
      "name": "Shariar",
      "department": "CSE",
      "cgpa": 3.75
    },
    {
      "id": 2,
      "name": "Rahim",
      "department": "EEE",
      "cgpa": 3.5
    }
  ]
}
```

JavaScript:

```javascript
const data = {
  success: true,
  students: [
    {
      id: 1,
      name: "Shariar",
      department: "CSE",
      cgpa: 3.75,
    },
    {
      id: 2,
      name: "Rahim",
      department: "EEE",
      cgpa: 3.5,
    },
  ],
};

data.students.forEach((student) => {
  console.log(student.name, student.cgpa);
});
```

Output:

```text
Shariar 3.75
Rahim 3.5
```

---

# 19.49 JSON + `map()`

```javascript
const names = data.students.map((student) => student.name);

console.log(names);
```

Output:

```text
["Shariar", "Rahim"]
```

---

# 19.50 JSON + `filter()`

CSE students:

```javascript
const cseStudents = data.students.filter(
  (student) => student.department === "CSE",
);

console.log(cseStudents);
```

Output:

```javascript
[
  {
    id: 1,
    name: "Shariar",
    department: "CSE",
    cgpa: 3.75,
  },
];
```

---

# 19.51 JSON + `find()`

```javascript
const student = data.students.find((student) => student.id === 1);

console.log(student);
```

Output:

```javascript
{
    id: 1,
    name: "Shariar",
    department: "CSE",
    cgpa: 3.75
}
```

---

# 19.52 Complete JSON Workflow

একটি real-world application:

```text
                 USER
                   ↓
               FRONTEND
                   ↓
           JavaScript Object
                   ↓
            JSON.stringify()
                   ↓
              HTTP Request
                   ↓
                API
                   ↓
               BACKEND
                   ↓
              DATABASE
                   ↓
              JSON Response
                   ↓
             response.json()
                   ↓
           JavaScript Object
                   ↓
                UI
```

এটাই modern web development-এর একটি core data flow।

---

# 🔥 19.53 Must Know

এই chapter-এর সবচেয়ে গুরুত্বপূর্ণ বিষয়গুলো:

### JSON Basics

```text
JSON
JSON Object
JSON Array
JSON String
JSON Number
JSON Boolean
JSON null
```

### Conversion

```javascript
JSON.stringify();
JSON.parse();
```

### Advanced

```javascript
replacer
reviver
pretty printing
```

### JSON Limitations

```text
No functions
No undefined
No comments
No native Date
No Map/Set
No circular references
```

### Web Development

```text
JSON + localStorage
JSON + fetch
JSON + REST API
JSON + HTTP
JSON + Backend
```

---

# 🧠 19.54 Quick Cheat Sheet

| কাজ                 | Code                             |
| ------------------- | -------------------------------- |
| Object → JSON       | `JSON.stringify(object)`         |
| Array → JSON        | `JSON.stringify(array)`          |
| JSON → Object       | `JSON.parse(json)`               |
| Pretty JSON         | `JSON.stringify(data, null, 2)`  |
| Filter properties   | `JSON.stringify(data, ["name"])` |
| Custom stringify    | `JSON.stringify(data, replacer)` |
| Custom parse        | `JSON.parse(json, reviver)`      |
| JSON API body       | `JSON.stringify(data)`           |
| JSON response parse | `response.json()`                |
| localStorage save   | `JSON.stringify(data)`           |
| localStorage read   | `JSON.parse(data)`               |

---

# 📝 19.55 Practice Set

### Practice 1 — Student Object

এই object তৈরি করো:

```javascript
const student = {
  name: "Shariar",
  id: "23105101019",
  department: "CSE",
  semester: 8,
  skills: ["HTML", "CSS", "JavaScript", "React"],
};
```

এটাকে JSON string-এ convert করো।

---

### Practice 2 — JSON → Object

এই JSON parse করো:

```json
{
  "name": "Shariar",
  "age": 24,
  "role": "Full Stack Developer"
}
```

তারপর:

```javascript
console.log(user.name);
console.log(user.role);
```

করো।

---

### Practice 3 — Pretty JSON

Nested object তৈরি করে:

```javascript
JSON.stringify(data, null, 2);
```

দিয়ে সুন্দর JSON output তৈরি করো।

---

### Practice 4 — Product API

এই structure ব্যবহার করো:

```javascript
const products = [
  {
    id: 1,
    name: "Laptop",
    price: 70000,
  },
  {
    id: 2,
    name: "Mouse",
    price: 1500,
  },
  {
    id: 3,
    name: "Keyboard",
    price: 2500,
  },
];
```

তারপর:

```text
→ JSON বানাও
→ JSON parse করো
→ price > 2000 filter করো
→ product names বের করো
```

---

### Practice 5 — localStorage

একটি user object:

```javascript
{
    name: "Shariar",
    email: "shariar@gmail.com",
    role: "Developer"
}
```

`localStorage`-এ save করো এবং page reload-এর পরে আবার object হিসেবে retrieve করো।

---

### Practice 6 — API Request Body

এই data:

```javascript
const user = {
  name: "Shariar",
  email: "shariar@gmail.com",
};
```

`fetch()` request-এর JSON body হিসেবে পাঠানোর code লেখো।

---

# 🎯 19.56 Final Mental Map

```text
                    JSON
                     │
          ┌──────────┴──────────┐
          ↓                     ↓
      JavaScript             JSON Text
       Object                   │
          │                     │
          │ stringify()         │
          ├────────────────────→│
          │                     │
          │                     │
          │←────────────────────┤
          │       parse()       │
          ↓                     ↓
       Object / Array         API
          │                     │
          ↓                     ↓
     localStorage          Backend
          │                     │
          └──────────┬──────────┘
                     ↓
                 Database
```

---

## ⭐ Chapter 19-এর মূল কথা

তুমি যদি পুরো chapter থেকে শুধু এই flow-টা ভালোভাবে বুঝে ফেলো:

```javascript
const user = {
  name: "Shariar",
  age: 24,
};

// JavaScript → JSON
const json = JSON.stringify(user);

console.log(json);

// JSON → JavaScript
const data = JSON.parse(json);

console.log(data.name);
```

তাহলে JSON-এর fundamental concept তোমার পরিষ্কার।

আর Full Stack Development-এ সবচেয়ে গুরুত্বপূর্ণ connection হলো:

```text
JavaScript Object
       ↓
JSON.stringify()
       ↓
API Request
       ↓
Backend
       ↓
JSON Response
       ↓
response.json()
       ↓
JavaScript Object
       ↓
React UI
```

**Chapter 19 complete.**
