# 📘 JavaScript Complete Book — Chapter 12

# Map, Set, WeakMap & WeakSet

### *(Advanced Collections — উন্নত Data Collection)*

Chapter 11-এ আমরা **Prototype, Prototype Chain, Constructor, Class ও Inheritance** শিখেছি।

এবার JavaScript-এর এমন কিছু data structure শিখব যেগুলো real-life application-এ অনেক কাজে লাগে:

```text
Map
Set
WeakMap
WeakSet
```

বিশেষ করে **Map** এবং **Set** খুব ভালোভাবে শেখা জরুরি। 🔥

---

# 12.1 Collection কী?

Collection মানে হলো:

> একসাথে অনেকগুলো data সুন্দরভাবে সংরক্ষণ করার structure।

আমরা আগে Array এবং Object শিখেছি।

যেমন:

```javascript
const students = ["Ripon", "Rahim", "Karim"];
```

এখানে Array ব্যবহার করেছি।

আবার:

```javascript
const student = {
    name: "Ripon",
    department: "CSE"
};
```

এখানে Object ব্যবহার করেছি।

কিন্তু কিছু ক্ষেত্রে Object/Array-এর চেয়ে `Map` বা `Set` বেশি সুবিধাজনক।

---

# 12.2 Map কী?

`Map` হলো এমন একটি collection যেখানে:

```text
key → value
```

pair হিসেবে data রাখা হয়।

### সহজভাবে:

```text
Map

key         value
---------------------
name   →    Ripon
age    →    25
dept   →    CSE
```

Object-এর মতো মনে হচ্ছে, কিন্তু Map অনেক বেশি flexible।

---

# 12.3 Map তৈরি করা

```javascript
const user = new Map();

console.log(user);
```

### Output:

```text
Map(0) {}
```

---

# 12.4 Map-এ Data যোগ করা — `set()`

Map-এ data যোগ করতে:

```javascript
set()
```

ব্যবহার করা হয়।

```javascript
const user = new Map();

user.set("name", "Ripon");
user.set("age", 25);
user.set("department", "CSE");

console.log(user);
```

### Output:

```text
Map(3) {
    "name" => "Ripon",
    "age" => 25,
    "department" => "CSE"
}
```

---

# 12.5 Map থেকে Data নেওয়া — `get()`

```javascript
const user = new Map();

user.set("name", "Ripon");
user.set("age", 25);

console.log(user.get("name"));
console.log(user.get("age"));
```

### Output:

```text
Ripon
25
```

### মনে রাখো:

```text
set() → data রাখে
get() → data বের করে
```

---

# 12.6 Map-এ Key না থাকলে

```javascript
const user = new Map();

user.set("name", "Ripon");

console.log(user.get("age"));
```

### Output:

```text
undefined
```

কারণ:

```text
age
```

key নেই।

---

# 12.7 `has()` — Key আছে কিনা

```javascript
const user = new Map();

user.set("name", "Ripon");

console.log(user.has("name"));
console.log(user.has("age"));
```

### Output:

```text
true
false
```

### Syntax:

```javascript
map.has(key);
```

---

# 12.8 `delete()` — Data Remove করা

```javascript
const user = new Map();

user.set("name", "Ripon");
user.set("age", 25);

user.delete("age");

console.log(user);
```

### Output:

```text
Map(1) {
    "name" => "Ripon"
}
```

---

# 12.9 `clear()` — সব Data Remove

```javascript
const user = new Map();

user.set("name", "Ripon");
user.set("age", 25);

user.clear();

console.log(user);
```

### Output:

```text
Map(0) {}
```

---

# 12.10 `size` — কতগুলো Item আছে

```javascript
const user = new Map();

user.set("name", "Ripon");
user.set("age", 25);
user.set("department", "CSE");

console.log(user.size);
```

### Output:

```text
3
```

খেয়াল করো:

```javascript
map.size
```

এখানে `()` নেই।

---

# 12.11 Map-এর Basic Methods

| Method     | কাজ                       |
| ---------- | ------------------------- |
| `set()`    | Data যোগ করে              |
| `get()`    | Data বের করে              |
| `has()`    | Key আছে কিনা দেখে         |
| `delete()` | নির্দিষ্ট item remove করে |
| `clear()`  | সব item remove করে        |
| `size`     | মোট item সংখ্যা           |

---

# 12.12 Map-এর Key শুধু String হতে হবে না

এটাই Map-এর বড় সুবিধাগুলোর একটি।

Object-এ সাধারণত property key string বা symbol-based হয়।

কিন্তু Map-এ key হিসেবে ব্যবহার করা যায়:

```text
String
Number
Boolean
Object
Array
Function
Symbol
```

ইত্যাদি।

---

# 12.13 Number Key

```javascript
const students = new Map();

students.set(101, "Ripon");
students.set(102, "Rahim");

console.log(students.get(101));
```

### Output:

```text
Ripon
```

---

# 12.14 Boolean Key

```javascript
const data = new Map();

data.set(true, "Logged In");
data.set(false, "Logged Out");

console.log(data.get(true));
```

### Output:

```text
Logged In
```

---

# 12.15 Object as Map Key

🔥 খুব গুরুত্বপূর্ণ।

```javascript
const user1 = {
    name: "Ripon"
};

const user2 = {
    name: "Rahim"
};

const scores = new Map();

scores.set(user1, 95);
scores.set(user2, 88);

console.log(scores.get(user1));
console.log(scores.get(user2));
```

### Output:

```text
95
88
```

এখানে object-গুলো নিজেরাই key।

---

# 12.16 Array as Map Key

```javascript
const map = new Map();

const numbers = [1, 2, 3];

map.set(numbers, "Numbers Array");

console.log(map.get(numbers));
```

### Output:

```text
Numbers Array
```

---

# 12.17 Function as Map Key

```javascript
function greet() {
    console.log("Hello");
}

const map = new Map();

map.set(greet, "Greeting Function");

console.log(map.get(greet));
```

### Output:

```text
Greeting Function
```

---

# 12.18 Map Constructor দিয়ে সরাসরি Data

Map তৈরি করার সময় initial data দেওয়া যায়।

```javascript
const user = new Map([
    ["name", "Ripon"],
    ["age", 25],
    ["department", "CSE"]
]);

console.log(user.get("name"));
```

### Output:

```text
Ripon
```

---

# 12.19 Map-এর উপর Loop

Map-এর সবচেয়ে common way:

```javascript
for...of
```

### Example

```javascript
const user = new Map([
    ["name", "Ripon"],
    ["age", 25],
    ["department", "CSE"]
]);

for (const [key, value] of user) {
    console.log(key, value);
}
```

### Output:

```text
name Ripon
age 25
department CSE
```

---

# 12.20 `keys()`

শুধু keys দেখতে:

```javascript
const user = new Map([
    ["name", "Ripon"],
    ["age", 25],
    ["department", "CSE"]
]);

for (const key of user.keys()) {
    console.log(key);
}
```

### Output:

```text
name
age
department
```

---

# 12.21 `values()`

শুধু values:

```javascript
for (const value of user.values()) {
    console.log(value);
}
```

### Output:

```text
Ripon
25
CSE
```

---

# 12.22 `entries()`

Key + value pair:

```javascript
for (const entry of user.entries()) {
    console.log(entry);
}
```

### Output:

```text
["name", "Ripon"]
["age", 25]
["department", "CSE"]
```

---

# 12.23 Map Destructuring

এভাবে সবচেয়ে সুন্দর:

```javascript
for (const [key, value] of user) {
    console.log(`${key}: ${value}`);
}
```

### Output:

```text
name: Ripon
age: 25
department: CSE
```

---

# 12.24 Map-এর Real-Life Example — Student Marks

ধরো Student ID দিয়ে marks store করব।

```javascript
const marks = new Map();

marks.set("CSE101", 85);
marks.set("CSE102", 90);
marks.set("CSE103", 78);

console.log(marks.get("CSE102"));
```

### Output:

```text
90
```

এখানে:

```text
Student ID → Marks
```

---

# 12.25 Map-এর Real-Life Example — Product Price

```javascript
const products = new Map();

products.set("Laptop", 75000);
products.set("Phone", 35000);
products.set("Keyboard", 2500);

console.log(products.get("Laptop"));
```

### Output:

```text
75000
```

---

# 12.26 Map ব্যবহার করে Frequency Counter

এটি খুব গুরুত্বপূর্ণ এবং interview-এও কাজে লাগে।

ধরো:

```javascript
const fruits = [
    "apple",
    "banana",
    "apple",
    "orange",
    "banana",
    "apple"
];
```

আমরা count করতে চাই।

```javascript
const count = new Map();

for (const fruit of fruits) {
    count.set(
        fruit,
        (count.get(fruit) || 0) + 1
    );
}

console.log(count);
```

### Output:

```text
Map(3) {
    "apple" => 3,
    "banana" => 2,
    "orange" => 1
}
```

🔥 এই pattern অনেক useful:

```javascript
map.set(
    key,
    (map.get(key) || 0) + 1
);
```

---

# 12.27 Set কী?

এখন আসি `Set`-এ।

> **Set হলো এমন একটি collection যেখানে duplicate value রাখা হয় না।**

### Example:

```javascript
const numbers = new Set();

numbers.add(10);
numbers.add(20);
numbers.add(10);

console.log(numbers);
```

### Output:

```text
Set(2) {
    10,
    20
}
```

দুইবার `10` দেওয়া হলেও একবারই আছে।

---

# 12.28 Set তৈরি করা

```javascript
const numbers = new Set();

console.log(numbers);
```

Output:

```text
Set(0) {}
```

---

# 12.29 `add()`

Set-এ data যোগ করতে:

```javascript
numbers.add(10);
numbers.add(20);
numbers.add(30);
```

---

# 12.30 Duplicate Data

```javascript
const numbers = new Set();

numbers.add(10);
numbers.add(10);
numbers.add(10);

console.log(numbers.size);
```

### Output:

```text
1
```

---

# 12.31 `has()`

```javascript
const numbers = new Set([10, 20, 30]);

console.log(numbers.has(20));
console.log(numbers.has(50));
```

### Output:

```text
true
false
```

---

# 12.32 `delete()`

```javascript
const numbers = new Set([10, 20, 30]);

numbers.delete(20);

console.log(numbers);
```

### Output:

```text
Set(2) {
    10,
    30
}
```

---

# 12.33 `clear()`

```javascript
const numbers = new Set([10, 20, 30]);

numbers.clear();

console.log(numbers);
```

### Output:

```text
Set(0) {}
```

---

# 12.34 `size`

```javascript
const numbers = new Set([10, 20, 30]);

console.log(numbers.size);
```

### Output:

```text
3
```

---

# 12.35 Set Constructor দিয়ে Array

```javascript
const numbers = new Set([10, 20, 30, 40]);

console.log(numbers);
```

### Output:

```text
Set(4) {
    10,
    20,
    30,
    40
}
```

---

# 12.36 Array থেকে Duplicate Remove

🔥 Set-এর সবচেয়ে common practical use।

ধরো:

```javascript
const numbers = [
    10,
    20,
    10,
    30,
    20,
    40
];
```

Duplicate remove:

```javascript
const uniqueNumbers = [...new Set(numbers)];

console.log(uniqueNumbers);
```

### Output:

```text
[10, 20, 30, 40]
```

---

# 12.37 Real-Life Example — Unique Skills

```javascript
const skills = [
    "HTML",
    "CSS",
    "JavaScript",
    "React",
    "JavaScript",
    "Node.js",
    "React"
];

const uniqueSkills = [...new Set(skills)];

console.log(uniqueSkills);
```

### Output:

```text
[
    "HTML",
    "CSS",
    "JavaScript",
    "React",
    "Node.js"
]
```

এটা real-life portfolio বা form data cleaning-এ কাজে লাগতে পারে।

---

# 12.38 Set-এর উপর Loop

```javascript
const skills = new Set([
    "HTML",
    "CSS",
    "JavaScript"
]);

for (const skill of skills) {
    console.log(skill);
}
```

### Output:

```text
HTML
CSS
JavaScript
```

---

# 12.39 Set-এর `keys()`, `values()`, `entries()`

Set-এর ক্ষেত্রে:

```javascript
set.keys()
set.values()
set.entries()
```

আছে।

### Example

```javascript
const numbers = new Set([10, 20, 30]);

console.log([...numbers.keys()]);
console.log([...numbers.values()]);
console.log([...numbers.entries()]);
```

### Output

```text
[10, 20, 30]

[10, 20, 30]

[
    [10, 10],
    [20, 20],
    [30, 30]
]
```

Set-এ key এবং value একই।

---

# 12.40 Set কি Object-এর মতো?

না।

### Object

```javascript
const user = {
    name: "Ripon",
    age: 25
};
```

Key-value pair:

```text
key → value
```

### Set

```javascript
const skills = new Set([
    "HTML",
    "CSS",
    "JS"
]);
```

শুধু unique values:

```text
value
value
value
```

---

# 12.41 Map বনাম Object

খুব গুরুত্বপূর্ণ comparison।

| বিষয়                 | Object                | Map          |
| -------------------- | --------------------- | ------------ |
| Key-value            | ✅                     | ✅            |
| Key type             | String/Symbol         | যেকোনো value |
| `size`               | সরাসরি নেই            | ✅            |
| `set()`              | ❌                     | ✅            |
| `get()`              | ❌                     | ✅            |
| `has()`              | আলাদা methods         | ✅            |
| Iteration            | তুলনামূলক less direct | সহজ          |
| Object key           | special handling      | সরাসরি       |
| Structured data      | খুব ভালো              | ভালো         |
| Dictionary-like data | ভালো                  | খুব ভালো     |

---

# 12.42 কখন Object ব্যবহার করব?

যখন data-এর structure fixed বা JSON/API-like হয়:

```javascript
const user = {
    name: "Ripon",
    email: "ripon@example.com",
    department: "CSE"
};
```

এখানে Object খুব natural।

---

# 12.43 কখন Map ব্যবহার করব?

যখন:

* Dynamic keys আছে
* Key হিসেবে object/array/function লাগবে
* Frequent add/remove দরকার
* Size জানতে হবে
* Key-value collection নিয়ে বেশি কাজ করতে হবে

তখন Map ভালো choice হতে পারে।

---

# 12.44 Set বনাম Array

| বিষয়          | Array               | Set      |
| ------------- | ------------------- | -------- |
| Duplicate     | ✅                   | ❌        |
| Index         | ✅                   | ❌        |
| `length`      | ✅                   | ❌        |
| `size`        | ❌                   | ✅        |
| `push()`      | ✅                   | ❌        |
| `add()`       | ❌                   | ✅        |
| Unique values | নিজে handle করতে হয় | Built-in |

---

# 12.45 WeakMap কী?

এখন advanced part।

`WeakMap` দেখতে Map-এর মতো, কিন্তু এর key-এর ক্ষেত্রে গুরুত্বপূর্ণ restriction আছে:

> **WeakMap-এর key অবশ্যই object বা non-registered Symbol হতে হবে।**

সবচেয়ে সহজভাবে object key দিয়ে example দেখি।

```javascript
const weakMap = new WeakMap();

const user = {
    name: "Ripon"
};

weakMap.set(user, "Private Data");

console.log(weakMap.get(user));
```

### Output:

```text
Private Data
```

---

# 12.46 WeakMap-এ String Key দেওয়া যাবে?

```javascript
const weakMap = new WeakMap();

weakMap.set("name", "Ripon");
```

এটি কাজ করবে না।

কারণ:

```text
WeakMap key → Object / non-registered Symbol
```

---

# 12.47 WeakMap-এর Basic Methods

WeakMap-এ:

```text
set()
get()
has()
delete()
```

আছে।

কিন্তু Map-এর মতো:

```text
size
keys()
values()
entries()
clear()
```

নেই।

---

# 12.48 WeakMap কেন "Weak"?

এখানে memory management-এর ধারণা আসে।

ধরো:

```javascript
let user = {
    name: "Ripon"
};

const weakMap = new WeakMap();

weakMap.set(user, "Private Data");
```

এখন `user` object-এর আর কোনো strong reference না থাকলে garbage collector ভবিষ্যতে object-টিকে memory থেকে remove করতে পারে।

WeakMap সেই object-কে জোর করে memory-তে ধরে রাখে না।

এজন্য:

> **WeakMap = weak reference behavior**

---

# 12.49 Garbage Collection-এর Basic Idea

JavaScript automatically unused objects-এর memory reclaim করতে পারে।

ধরো:

```javascript
let user = {
    name: "Ripon"
};
```

তারপর:

```javascript
user = null;
```

যদি object-টির আর কোনো reachable reference না থাকে, garbage collector ভবিষ্যতে সেটিকে remove করতে পারে।

এটি JavaScript-এর automatic memory management-এর অংশ।

---

# 12.50 WeakMap Real-Life Use

WeakMap ব্যবহার করা যায় object-এর সাথে extra/private metadata রাখার ক্ষেত্রে।

```javascript
const privateData = new WeakMap();

class User {
    constructor(name, password) {
        this.name = name;

        privateData.set(this, {
            password: password
        });
    }

    checkPassword(password) {
        return privateData.get(this).password === password;
    }
}

const user = new User(
    "Ripon",
    "12345"
);

console.log(user.checkPassword("12345"));
```

### Output

```text
true
```

এখানে password সরাসরি:

```javascript
user.password
```

হিসেবে রাখা হয়নি।

---

# 12.51 WeakSet কী?

`WeakSet` হলো Set-এর weak-reference version।

এখানে সাধারণত object রাখা হয়।

```javascript
const weakSet = new WeakSet();

const user1 = {
    name: "Ripon"
};

const user2 = {
    name: "Rahim"
};

weakSet.add(user1);
weakSet.add(user2);

console.log(weakSet.has(user1));
```

### Output:

```text
true
```

---

# 12.52 WeakSet-এ Primitive রাখা যাবে?

না।

```javascript
const weakSet = new WeakSet();

weakSet.add(10);
```

এটি কাজ করবে না।

WeakSet-এ object বা non-registered Symbol ধরনের value ব্যবহার করা যায়।

---

# 12.53 WeakSet-এর Methods

মূল methods:

```text
add()
has()
delete()
```

`WeakSet`-এ:

```text
size
keys()
values()
entries()
clear()
```

নেই।

---

# 12.54 WeakMap বনাম Map

| বিষয়                        | Map          | WeakMap                        |
| --------------------------- | ------------ | ------------------------------ |
| Key                         | যেকোনো value | Object / non-registered Symbol |
| `get()`                     | ✅            | ✅                              |
| `set()`                     | ✅            | ✅                              |
| `has()`                     | ✅            | ✅                              |
| `delete()`                  | ✅            | ✅                              |
| `size`                      | ✅            | ❌                              |
| Iteration                   | ✅            | ❌                              |
| Garbage collection friendly | সাধারণ       | বেশি                           |
| Object metadata             | ভালো         | খুব useful                     |

---

# 12.55 WeakSet বনাম Set

| বিষয়        | Set          | WeakSet                        |
| ----------- | ------------ | ------------------------------ |
| Values      | যেকোনো value | Object / non-registered Symbol |
| Duplicate   | ❌            | ❌                              |
| `add()`     | ✅            | ✅                              |
| `has()`     | ✅            | ✅                              |
| `delete()`  | ✅            | ✅                              |
| `size`      | ✅            | ❌                              |
| Iteration   | ✅            | ❌                              |
| GC-friendly | সাধারণ       | বেশি                           |

---

# 12.56 Map + Set Practical Project

এবার একটা ছোট বাস্তব example করি।

ধরো আমরা একটা online course platform বানাচ্ছি।

একজন user কোন কোন course enroll করেছে তা রাখতে চাই।

```javascript
const enrolledCourses = new Map();

const user1 = {
    id: 101,
    name: "Ripon"
};

enrolledCourses.set(user1, new Set([
    "JavaScript",
    "React",
    "Node.js"
]));

console.log(
    enrolledCourses.get(user1)
);
```

### Output

```text
Set(3) {
    "JavaScript",
    "React",
    "Node.js"
}
```

এখানে:

```text
Map
 ↓
User → Courses

Set
 ↓
Unique Courses
```

---

# 12.57 User-এর Course Check

```javascript
const courses = enrolledCourses.get(user1);

console.log(courses.has("React"));
console.log(courses.has("Python"));
```

### Output:

```text
true
false
```

---

# 12.58 Course Add করা

```javascript
courses.add("Python");

console.log(courses);
```

### Output:

```text
Set(4) {
    "JavaScript",
    "React",
    "Node.js",
    "Python"
}
```

---

# 12.59 Duplicate Course Add

```javascript
courses.add("React");

console.log(courses.size);
```

### Output:

```text
4
```

কারণ React আগেই ছিল।

---

# 12.60 Map + Set Mental Model

এটা মনে রাখার মতো:

```text
                Map
                 │
                 ▼
          User → Set
                 │
       ┌─────────┼─────────┐
       ▼         ▼         ▼
   JavaScript   React    Node.js
```

এ ধরনের nested data structure real application-এ ব্যবহার করা যায়।

---

# 🔥 12.61 Must Know

Chapter 12 থেকে অবশ্যই জানতে হবে:

### Map

```javascript
const map = new Map();

map.set("name", "Ripon");
map.get("name");
map.has("name");
map.delete("name");
map.clear();

map.size;
```

### Set

```javascript
const set = new Set();

set.add("JavaScript");
set.has("JavaScript");
set.delete("JavaScript");
set.clear();

set.size;
```

### Duplicate Remove

```javascript
const unique = [...new Set(array)];
```

### WeakMap

```javascript
const weakMap = new WeakMap();

weakMap.set(object, value);
weakMap.get(object);
weakMap.has(object);
weakMap.delete(object);
```

### WeakSet

```javascript
const weakSet = new WeakSet();

weakSet.add(object);
weakSet.has(object);
weakSet.delete(object);
```

---

# 🧠 Quick Cheat Sheet

```text
Object
→ Key-value data
→ সাধারণ structured data

Map
→ Key-value data
→ Any type of key
→ Dynamic collection

Set
→ Unique values
→ Duplicate automatically removed

WeakMap
→ Object/non-registered Symbol keys
→ Weak reference behavior
→ No iteration/size

WeakSet
→ Object/non-registered Symbol values
→ Weak reference behavior
→ No iteration/size
```

---

# 🎯 কখন কোনটা ব্যবহার করব?

```text
আমার data যদি...

Key → Value
        ↓
      Object

Dynamic Key → Value
        ↓
       Map

শুধু Unique Values
        ↓
       Set

Object-এর সাথে extra metadata
        ↓
      WeakMap

Object-গুলো tracked/marked রাখতে চাই
        ↓
      WeakSet
```

---

# 📝 Practice Set

### Practice 1 — Map

একটি Map বানাও:

```text
101 → Ripon
102 → Rahim
103 → Karim
```

তারপর `102` দিয়ে নাম বের করো।

---

### Practice 2 — Set

এই Array থেকে duplicate remove করো:

```javascript
const skills = [
    "HTML",
    "CSS",
    "JS",
    "React",
    "JS",
    "CSS",
    "Node"
];
```

Expected:

```text
[
    "HTML",
    "CSS",
    "JS",
    "React",
    "Node"
]
```

---

### Practice 3 — Frequency Counter

এই Array-তে প্রত্যেক number কতবার আছে বের করো:

```javascript
const numbers = [
    1, 2, 3, 1, 2, 1, 4, 3
];
```

Expected:

```text
1 → 3
2 → 2
3 → 2
4 → 1
```

---

### Practice 4 — Object as Map Key

দুইজন user object তৈরি করো:

```javascript
const user1 = {};
const user2 = {};
```

তারপর Map ব্যবহার করে তাদের score রাখো।

---

### Practice 5 — Map + Set

একটি Map বানাও যেখানে:

```text
Student → Courses
```

যেমন:

```text
Ripon → HTML, CSS, JavaScript
Rahim → JavaScript, React
```

তারপর check করো:

```text
Ripon কি React course-এ আছে?
```

---

# 🚀 Chapter 12 Final Summary

আজ আমরা শিখলাম:

```text
Map
 ↓
set()
get()
has()
delete()
clear()
size
keys()
values()
entries()
```

তারপর:

```text
Set
 ↓
add()
has()
delete()
clear()
size
```

তারপর advanced:

```text
WeakMap
 ↓
Object → Metadata
 ↓
Weak Reference
```

এবং:

```text
WeakSet
 ↓
Objects track করা
 ↓
Weak Reference
```

সবচেয়ে গুরুত্বপূর্ণ ৪টি লাইন মনে রাখো:

```javascript
const map = new Map();

const set = new Set();

const weakMap = new WeakMap();

const weakSet = new WeakSet();
```

আর practical JavaScript-এ সবচেয়ে বেশি মনে রাখার মতো দুইটি pattern:

```javascript
// Duplicate remove
const unique = [...new Set(array)];
```

এবং:

```javascript
// Frequency counter
map.set(key, (map.get(key) || 0) + 1);
```

**Chapter 13**-এ আমরা এরপর **JavaScript Built-in Objects — `Date`, `Math`, `Number`, `String`, `RegExp`, `Intl`, `BigInt`** বিস্তারিতভাবে শিখতে পারি।
