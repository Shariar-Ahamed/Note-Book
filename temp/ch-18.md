# 📘 Chapter 18 — Browser Storage & Web Storage APIs

### Browser Storage — Browser-এর মধ্যে Data Save, Read, Update ও Delete করা

JavaScript-এর Chapter 17-এ আমরা Form থেকে user-এর data নেওয়া শিখেছি। এখন প্রশ্ন হলো—

> **Form থেকে নেওয়া data কোথায় রাখব?**

কিছু data browser-এর মধ্যেই temporary বা persistentভাবে রাখা যায়। এজন্য JavaScript-এ সবচেয়ে গুরুত্বপূর্ণ browser storage technologies হলো:

```text
localStorage
sessionStorage
Cookies
IndexedDB
```

এই chapter-এ আমরা এগুলো এবং practical use case শিখব।

---

# 18.1 Browser Storage কী?

Browser Storage হলো browser-এর মধ্যে website-এর কিছু data সংরক্ষণ করার ব্যবস্থা।

### Real-life example

ধরো তুমি একটি shopping website-এ গেলে এবং:

```text
Dark Mode → ON
Language → বাংলা
Cart → 3 products
```

Browser কিছু information মনে রাখতে পারে।

তাই পরেরবার website খুললে:

```text
Dark Mode → ON
Language → বাংলা
```

থাকতে পারে।

---

# 18.2 Web Storage API

JavaScript-এর Web Storage API-এর প্রধান দুটি অংশ:

```text
localStorage
sessionStorage
```

দুটোই সাধারণত **key-value pair** আকারে data রাখে।

```text
key          value
-------------------------
username     Shariar
theme        dark
language     bn
```

---

# 18.3 `localStorage` কী?

`localStorage` browser-এ data save করে এবং সাধারণত browser বন্ধ করলেও data থেকে যায়।

```javascript id="8a6d6e"
localStorage.setItem("username", "Shariar");
```

এখন browser-এ:

```text
username = Shariar
```

save হয়েছে।

---

# 18.4 `localStorage.setItem()`

Syntax:

```javascript id="k0x3cg"
localStorage.setItem(key, value);
```

Example:

```javascript id="8d4f8f"
localStorage.setItem("name", "Shariar");
```

আরেকটি:

```javascript id="m4u8fa"
localStorage.setItem("theme", "dark");
```

---

# 18.5 `localStorage.getItem()`

Data পড়তে:

```javascript id="r5m9cm"
const name = localStorage.getItem("name");

console.log(name);
```

Output:

```text id="r81zcn"
Shariar
```

---

# 18.6 `localStorage.removeItem()`

একটি নির্দিষ্ট item delete:

```javascript id="d6a8lh"
localStorage.removeItem("name");
```

এখন:

```javascript id="k7i8jy"
console.log(localStorage.getItem("name"));
```

Output:

```text id="2b8byv"
null
```

---

# 18.7 `localStorage.clear()`

সব localStorage data delete করতে:

```javascript id="q6g5t4"
localStorage.clear();
```

⚠️ এটি ওই origin-এর localStorage-এর **সব entries** মুছে দিতে পারে।

তাই production code-এ সাবধানে ব্যবহার করতে হবে।

---

# 18.8 `localStorage.length`

কতগুলো item আছে:

```javascript id="3imjbd"
console.log(localStorage.length);
```

যদি থাকে:

```text
name
theme
language
```

তাহলে:

```text id="v49e47"
3
```

---

# 18.9 `localStorage.key()`

Index দিয়ে key পাওয়া যায়।

```javascript id="h11n1f"
console.log(localStorage.key(0));
```

Output উদাহরণ:

```text id="l3j0p7"
name
```

⚠️ Key-এর order-এর উপর application logic নির্ভর করানো উচিত নয়।

---

# 18.10 localStorage-এর সবচেয়ে গুরুত্বপূর্ণ বৈশিষ্ট্য

```text id="3vqqph"
localStorage
     │
     ├── Key → Value
     ├── Persistent
     ├── Same origin
     └── String-based
```

সবচেয়ে গুরুত্বপূর্ণ:

> **localStorage মূলত string data store করে।**

---

# 18.11 Number Save করলে কী হয়?

```javascript id="u4gk6e"
localStorage.setItem("age", 25);
```

পরে:

```javascript id="1a7kcc"
const age = localStorage.getItem("age");

console.log(age);
console.log(typeof age);
```

Output:

```text id="w1p5r4"
25
string
```

অর্থাৎ:

```text
25 Number
   ↓
localStorage
   ↓
"25" String
```

---

# 18.12 String থেকে Number

```javascript id="7q4hbr"
const age = Number(localStorage.getItem("age"));

console.log(age);
console.log(typeof age);
```

Output:

```text id="c3q0j1"
25
number
```

---

# 18.13 Boolean Save করলে?

```javascript id="0k5w3f"
localStorage.setItem("isLoggedIn", true);
```

পরে:

```javascript id="u1qv1r"
const value = localStorage.getItem("isLoggedIn");

console.log(value);
console.log(typeof value);
```

Output:

```text id="d2pk6j"
true
string
```

⚠️ এটি Boolean `true` নয়, string `"true"`।

---

# 18.14 Boolean Convert করা

```javascript id="h7d9b3"
const isLoggedIn = localStorage.getItem("isLoggedIn") === "true";

console.log(isLoggedIn);
```

Output:

```text id="4m4a6k"
true
```

---

# 18.15 Object কি সরাসরি Save করা যায়?

ধরো:

```javascript id="qk0h2x"
const user = {
  name: "Shariar",
  age: 24,
};
```

এভাবে করলে:

```javascript id="qj7vko"
localStorage.setItem("user", user);
```

সমস্যা হবে।

পরে:

```javascript id="n1q0c2"
console.log(localStorage.getItem("user"));
```

Output হতে পারে:

```text id="3n7y4q"
[object Object]
```

কারণ Web Storage string-based।

---

# 18.16 Object Save করার জন্য `JSON.stringify()`

```javascript id="p3b6u5"
const user = {
  name: "Shariar",
  age: 24,
  role: "Developer",
};

localStorage.setItem("user", JSON.stringify(user));
```

এখন object string JSON format-এ save হয়েছে।

---

# 18.17 Object Read করার জন্য `JSON.parse()`

```javascript id="9f7q0s"
const storedUser = localStorage.getItem("user");

const user = JSON.parse(storedUser);

console.log(user);
```

Output:

```javascript id="9d5h6u"
{
    name: "Shariar",
    age: 24,
    role: "Developer"
}
```

এখন:

```javascript id="v6z2bc"
console.log(user.name);
```

Output:

```text id="0wq8m3"
Shariar
```

---

# 18.18 JSON + localStorage Formula

এটা খুব ভালোভাবে মনে রাখো:

### Save:

```javascript id="d3k2fs"
localStorage.setItem("user", JSON.stringify(user));
```

### Read:

```javascript id="r8d6wv"
const user = JSON.parse(localStorage.getItem("user"));
```

### Mental Model

```text
Object
   ↓
JSON.stringify()
   ↓
String
   ↓
localStorage
```

Read করার সময়:

```text
localStorage
   ↓
String
   ↓
JSON.parse()
   ↓
Object
```

---

# 18.19 Array Save করা

```javascript id="x7q1u9"
const skills = ["HTML", "CSS", "JavaScript", "React"];

localStorage.setItem("skills", JSON.stringify(skills));
```

Read:

```javascript id="w8p4dm"
const skills = JSON.parse(localStorage.getItem("skills"));

console.log(skills);
```

Output:

```text id="8q1xj7"
["HTML", "CSS", "JavaScript", "React"]
```

---

# 18.20 Array-এর মধ্যে নতুন Data যোগ করা

ধরো:

```javascript id="j0z6p4"
const skills = JSON.parse(localStorage.getItem("skills"));

skills.push("Node.js");

localStorage.setItem("skills", JSON.stringify(skills));
```

এখন storage-এ:

```text
HTML
CSS
JavaScript
React
Node.js
```

---

# 18.21 Safe JSON Parsing

যদি storage-এ কিছু না থাকে:

```javascript id="v6m4x0"
const user = JSON.parse(localStorage.getItem("user"));
```

`getItem()` যদি `null` দেয়, তাহলে `JSON.parse(null)` JavaScript-এ `null` দেয়, কিন্তু malformed JSON থাকলে error হতে পারে।

Safe pattern:

```javascript id="4j6f5u"
const storedUser = localStorage.getItem("user");

let user = null;

if (storedUser) {
  try {
    user = JSON.parse(storedUser);
  } catch (error) {
    console.error("Invalid stored user data", error);
  }
}

console.log(user);
```

---

# 18.22 `sessionStorage` কী?

`sessionStorage`-ও key-value storage।

```javascript id="p9j7o3"
sessionStorage.setItem("username", "Shariar");
```

Read:

```javascript id="y8d3q1"
console.log(sessionStorage.getItem("username"));
```

---

# 18.23 localStorage vs sessionStorage

| Feature         | `localStorage`  | `sessionStorage`                             |
| --------------- | --------------- | -------------------------------------------- |
| Data format     | String          | String                                       |
| Persistent      | Yes             | Session-based                                |
| Tab-specific    | No              | Yes                                          |
| Browser restart | Usually remains | Typically cleared when the page session ends |
| API             | Same            | Same                                         |
| `setItem()`     | Yes             | Yes                                          |
| `getItem()`     | Yes             | Yes                                          |
| `removeItem()`  | Yes             | Yes                                          |
| `clear()`       | Yes             | Yes                                          |

### সহজভাবে

```text
localStorage
→ "পরের বারও মনে রাখো"

sessionStorage
→ "এই tab/session-এর জন্য রাখো"
```

---

# 18.24 sessionStorage Example

ধরো একটি checkout process:

```text id="4n7o6e"
Step 1 → Personal Info
Step 2 → Address
Step 3 → Payment
```

Current tab-এর session-এর মধ্যে temporary state রাখতে:

```javascript id="o2c5n8"
sessionStorage.setItem("checkoutStep", "2");
```

পরে:

```javascript id="j6x1vb"
console.log(sessionStorage.getItem("checkoutStep"));
```

Output:

```text id="3d4n7x"
2
```

---

# 18.25 Theme Switcher — Real-life Example

এখন localStorage-এর একটি খুব common use case দেখি।

HTML:

```html id="8f1k2a"
<button id="themeBtn">Toggle Theme</button>
```

JavaScript:

```javascript id="k8m0x2"
const button = document.querySelector("#themeBtn");

button.addEventListener("click", function () {
  document.body.classList.toggle("dark");

  const isDark = document.body.classList.contains("dark");

  localStorage.setItem("theme", isDark ? "dark" : "light");
});
```

Page reload হলেও theme preference পাওয়া যাবে।

---

# 18.26 Page Load-এর সময় Theme Restore

শুধু save করলে হবে না। Page load হলে read করতে হবে।

```javascript id="7m2x1c"
const savedTheme = localStorage.getItem("theme");

if (savedTheme === "dark") {
  document.body.classList.add("dark");
}
```

### Complete Flow

```text id="l4j5e0"
User selects Dark Mode
        ↓
localStorage.setItem()
        ↓
Page reload
        ↓
localStorage.getItem()
        ↓
Dark Mode restore
```

---

# 18.27 Dark Mode Example

HTML:

```html id="k8c2yr"
<button id="themeBtn">Dark Mode</button>
```

CSS:

```css id="f9p4x2"
.dark {
  background: #111;
  color: white;
}
```

JavaScript:

```javascript id="b4j9p0"
const button = document.querySelector("#themeBtn");

const savedTheme = localStorage.getItem("theme");

if (savedTheme === "dark") {
  document.body.classList.add("dark");
}

button.addEventListener("click", function () {
  document.body.classList.toggle("dark");

  const theme = document.body.classList.contains("dark") ? "dark" : "light";

  localStorage.setItem("theme", theme);
});
```

এটি real-world website-এ খুব common pattern।

---

# 18.28 Shopping Cart with localStorage

ধরো:

```javascript id="q3z6r8"
const cart = [
  {
    id: 1,
    name: "Laptop",
    price: 70000,
  },
  {
    id: 2,
    name: "Mouse",
    price: 1500,
  },
];
```

Save:

```javascript id="j7y2m4"
localStorage.setItem("cart", JSON.stringify(cart));
```

Read:

```javascript id="z9x4q1"
const cartData = JSON.parse(localStorage.getItem("cart")) || [];

console.log(cartData);
```

এখানে:

```javascript id="a3p8k5"
|| []
```

এর কারণে storage-এ cart না থাকলে empty array পাওয়া যাবে।

---

# 18.29 Add Product to Cart

```javascript id="b8x5n2"
const product = {
  id: 3,
  name: "Keyboard",
  price: 2500,
};

const cart = JSON.parse(localStorage.getItem("cart")) || [];

cart.push(product);

localStorage.setItem("cart", JSON.stringify(cart));
```

---

# 18.30 Remove Product from Cart

```javascript id="w5h2p7"
let cart = JSON.parse(localStorage.getItem("cart")) || [];

cart = cart.filter((product) => product.id !== 2);

localStorage.setItem("cart", JSON.stringify(cart));
```

---

# 18.31 localStorage দিয়ে User Preferences

একটি website-এ:

```javascript id="n7d3q8"
const settings = {
  theme: "dark",
  language: "bn",
  fontSize: "large",
};

localStorage.setItem("settings", JSON.stringify(settings));
```

Read:

```javascript id="q5z9r2"
const settings = JSON.parse(localStorage.getItem("settings"));

console.log(settings.language);
```

Output:

```text id="w2k6m1"
bn
```

---

# 18.32 Storage-এ Sensitive Data রাখা যাবে?

এখানে **খুব গুরুত্বপূর্ণ security concept** আছে।

`localStorage`-এ sensitive information রাখা risky হতে পারে।

বিশেষ করে:

```text
Password
Long-lived secret
Highly sensitive personal information
Authentication secrets
```

কারণ JavaScript একই origin-এর storage access করতে পারে এবং XSS vulnerability থাকলে attacker storage-এর data পড়তে পারে।

### তাই:

❌ Password localStorage-এ রেখো না।

```javascript id="j3v7n1"
localStorage.setItem("password", "12345678");
```

এটা করা উচিত নয়।

---

# 18.33 Authentication Token নিয়ে সতর্কতা

অনেক web application authentication-এর জন্য token ব্যবহার করে।

যেমন:

```text
Access Token
Refresh Token
```

এগুলো কোথায় রাখা হবে—এটি security architecture-এর গুরুত্বপূর্ণ বিষয়।

বিশেষ করে authentication token localStorage-এ রাখার আগে **XSS risk** বিবেচনা করতে হয়।

Sensitive authentication cookie-এর ক্ষেত্রে server-managed:

```text
HttpOnly
Secure
SameSite
```

cookie attributes ব্যবহার করা যায়।

---

# 18.34 Cookies কী?

Cookie হলো browser-এ ছোট data store করার একটি mechanism।

Example:

```text id="s6q4v9"
theme=dark
language=bn
```

JavaScript দিয়ে:

```javascript id="p1y5d8"
document.cookie = "theme=dark";
```

Read:

```javascript id="g9f2k3"
console.log(document.cookie);
```

Output:

```text id="6q7m2b"
theme=dark
```

---

# 18.35 Cookie vs localStorage

| Feature                               | localStorage                   | Cookie                                                 |
| ------------------------------------- | ------------------------------ | ------------------------------------------------------ |
| Storage                               | Client-side                    | Client-side                                            |
| JavaScript access                     | Yes                            | Usually yes, unless `HttpOnly`                         |
| Automatically sent with HTTP requests | No                             | Yes, subject to cookie rules                           |
| Size                                  | তুলনামূলক বেশি                 | ছোট                                                    |
| Expiration                            | সাধারণত explicit expiration নয় | Expiration সেট করা যায়                                 |
| Server access                         | Directly নয়                    | Request-এর সাথে পাঠানো যায়                             |
| Common use                            | Preferences, local app state   | Session/auth-related state, server-managed preferences |

---

# 18.36 Cookie তৈরি করা

```javascript id="q6s1v8"
document.cookie = "theme=dark; max-age=3600; path=/";
```

এখানে:

```text
theme=dark
max-age=3600 seconds
path=/
```

---

# 18.37 Cookie Delete

একটি cookie expire করাতে:

```javascript id="z2r7k4"
document.cookie = "theme=; max-age=0; path=/";
```

---

# 18.38 Cookie-এর Important Attributes

### `Max-Age`

```text
max-age=3600
```

কত seconds পরে expire করবে।

### `Expires`

নির্দিষ্ট expiry time দেওয়া যায়।

### `Path`

```text
path=/
```

কোন path-এ cookie applicable।

### `Secure`

HTTPS connection-এর মাধ্যমে cookie পাঠাতে নির্দেশ দেয়।

### `HttpOnly`

JavaScript থেকে cookie access বন্ধ করে।

⚠️ `HttpOnly` cookie JavaScript দিয়ে `document.cookie` থেকে পড়া যায় না।

### `SameSite`

Cross-site request-এর ক্ষেত্রে cookie behavior নিয়ন্ত্রণে সাহায্য করে।

---

# 18.39 IndexedDB কী?

`localStorage` ছোট/simple data-এর জন্য ভালো।

কিন্তু যদি বড় বা structured client-side data রাখতে হয়, তখন:

```text
IndexedDB
```

ব্যবহার করা যায়।

এটি browser-এর একটি **asynchronous, transactional, structured database API**।

---

# 18.40 IndexedDB কখন ব্যবহার করব?

ধরো তুমি বানাচ্ছো:

```text
Offline Notes App
Offline Video Metadata App
Large Client-side Dataset
Offline-first Web App
```

এখানে হাজার হাজার structured record থাকতে পারে।

তখন `localStorage` ideal নয়।

---

# 18.41 localStorage vs IndexedDB

| বিষয়            | localStorage | IndexedDB       |
| --------------- | ------------ | --------------- |
| Data type       | String       | Structured data |
| API             | Simple       | More advanced   |
| Async           | No           | Yes             |
| Large data      | Not suitable | Better          |
| Transactions    | No           | Yes             |
| Queries/indexes | Very limited | Supported       |
| Complexity      | Easy         | More complex    |

---

# 18.42 IndexedDB-এর Basic Idea

Conceptually:

```text id="y8f1w3"
Database
   ↓
Object Store
   ↓
Records
```

উদাহরণ:

```text
Database: StudentDB

Object Store: students

1 → Shariar
2 → Rahim
3 → Karim
```

IndexedDB asynchronous হওয়ায় এর code localStorage-এর তুলনায় বেশি complex।

---

# 18.43 Storage Events

একটি interesting browser event হলো:

```javascript id="v5n9q2"
window.addEventListener("storage", function (event) {
  console.log(event.key);
  console.log(event.oldValue);
  console.log(event.newValue);
});
```

`storage` event সাধারণত অন্য same-origin document/context-এ storage পরিবর্তন হলে useful হয়।

---

# 18.44 Storage Event Example

ধরো একই website-এর দুইটি tab খোলা:

```text id="r6w2m1"
Tab A                 Tab B
  │                     │
  │ localStorage.set   │
  │───────────────────→│
  │                     │
  │                  storage event
```

Tab B পরিবর্তন detect করতে পারে।

---

# 18.45 Storage Data Inspect করা

Chrome DevTools-এ:

```text
DevTools
   ↓
Application
   ↓
Storage
   ↓
Local Storage
```

এখানে দেখতে পারবে:

```text
Key          Value
-------------------------
theme        dark
username     Shariar
cart         [...]
```

এটি debugging-এর জন্য খুব useful।

---

# 18.46 localStorage Utility Function

বারবার code না লিখে function তৈরি করতে পারো।

```javascript id="j6m3q8"
function saveData(key, value) {
  localStorage.setItem(key, JSON.stringify(value));
}
```

Use:

```javascript id="x9k2p1"
saveData("user", {
  name: "Shariar",
  age: 24,
});
```

---

# 18.47 Generic Read Function

```javascript id="c5r7v2"
function getData(key) {
  const data = localStorage.getItem(key);

  if (!data) {
    return null;
  }

  try {
    return JSON.parse(data);
  } catch (error) {
    console.error("Invalid JSON:", error);

    return null;
  }
}
```

Use:

```javascript id="p2y8m4"
const user = getData("user");

console.log(user);
```

এটি real project-এ utility হিসেবে ব্যবহার করা যায়।

---

# 18.48 Storage Wrapper

আরও সুন্দরভাবে:

```javascript id="r4k7w1"
const storage = {
  set(key, value) {
    localStorage.setItem(key, JSON.stringify(value));
  },

  get(key) {
    const value = localStorage.getItem(key);

    if (value === null) {
      return null;
    }

    try {
      return JSON.parse(value);
    } catch {
      return null;
    }
  },

  remove(key) {
    localStorage.removeItem(key);
  },

  clear() {
    localStorage.clear();
  },
};
```

Use:

```javascript id="y3m6q9"
storage.set("user", {
  name: "Shariar",
  role: "Developer",
});
```

Read:

```javascript id="n8v2c5"
console.log(storage.get("user"));
```

---

# 18.49 Storage-এর Limitation

localStorage খুব useful হলেও সবকিছুর জন্য নয়।

### Limitations:

```text
1. String-based
2. Synchronous API
3. Limited storage capacity
4. Same-origin scoped
5. XSS risk
6. Not suitable for large databases
7. Not suitable for secrets
```

---

# 🔥 18.50 Must Know

এই chapter থেকে অবশ্যই জানতে হবে:

```javascript id="4z3j8m"
localStorage.setItem();
```

```javascript id="0x7k1q"
localStorage.getItem();
```

```javascript id="m5p2r9"
localStorage.removeItem();
```

```javascript id="s8v3n6"
localStorage.clear();
```

```javascript id="e2q7h4"
localStorage.length;
```

```javascript id="u9w1k5"
localStorage.key();
```

---

### JSON Storage

```javascript id="n3c6v8"
JSON.stringify();
```

```javascript id="r7m2x4"
JSON.parse();
```

---

### Session

```javascript id="a5k8p1"
sessionStorage.setItem();
```

```javascript id="z4q9n2"
sessionStorage.getItem();
```

---

### Cookies

```javascript id="b6w3j7"
document.cookie;
```

---

### Advanced Storage

```text
IndexedDB
```

---

# 🧠 18.51 Quick Cheat Sheet

| কাজ                   | Code                                   |
| --------------------- | -------------------------------------- |
| Save                  | `localStorage.setItem("key", "value")` |
| Read                  | `localStorage.getItem("key")`          |
| Delete one            | `localStorage.removeItem("key")`       |
| Delete all            | `localStorage.clear()`                 |
| Count                 | `localStorage.length`                  |
| Get key               | `localStorage.key(index)`              |
| Object → String       | `JSON.stringify(obj)`                  |
| String → Object       | `JSON.parse(str)`                      |
| Session save          | `sessionStorage.setItem()`             |
| Session read          | `sessionStorage.getItem()`             |
| Cookie                | `document.cookie`                      |
| Large structured data | `IndexedDB`                            |

---

# 📝 18.52 Practice Set

### Practice 1 — Save User Name

Input থেকে name নিয়ে:

```text
localStorage
```

এ save করো।

Page reload করার পর name আবার দেখাও।

---

### Practice 2 — Dark Mode

একটি button বানাও:

```text
🌙 Dark Mode
☀️ Light Mode
```

User preference localStorage-এ save করো।

---

### Practice 3 — Shopping Cart

এই structure ব্যবহার করো:

```javascript id="w7p3k9"
{
    id: 1,
    name: "Laptop",
    price: 70000
}
```

Features:

```text
Add Product
Remove Product
Clear Cart
Cart Count
Total Price
```

সব data localStorage-এ রাখো।

---

### Practice 4 — Todo App

Features:

```text
Add Todo
Delete Todo
Mark Complete
Clear All
```

Page reload করলেও todo যেন থাকে।

---

### Practice 5 — Student Profile

Save:

```javascript id="c9r4m2"
{
    name: "Shariar",
    studentId: "23105101019",
    department: "CSE",
    university: "Daffodil International University"
}
```

localStorage-এ save করে reload-এর পর UI-তে দেখাও।

---

### Practice 6 — Session Storage

একটি multi-step form বানাও:

```text
Step 1 → Personal Information
Step 2 → Academic Information
Step 3 → Confirmation
```

Current step `sessionStorage`-এ রাখো।

---

# 🎯 18.53 Final Mental Map

```text id="n5x7c2"
              BROWSER STORAGE
                     │
       ┌─────────────┼─────────────┐
       ↓             ↓             ↓
 localStorage   sessionStorage   Cookies
       │             │             │
       ↓             ↓             ↓
 Persistent       Session       Request
 Client Data      Data          related
       │
       ↓
  String-based
       │
       ↓
 JSON.stringify()
       ↓
    Storage
       ↓
 JSON.parse()
       ↓
 Object / Array
```

আর বড় structured offline data হলে:

```text id="k2m8v4"
Large / Structured Data
          ↓
      IndexedDB
```

---

## ⭐ Chapter 18-এর সবচেয়ে গুরুত্বপূর্ণ Concept

তুমি যদি শুধু **একটি জিনিস** মনে রাখতে চাও:

```javascript id="d8q4m1"
const user = {
  name: "Shariar",
  role: "Developer",
};

// Save
localStorage.setItem("user", JSON.stringify(user));

// Read
const savedUser = JSON.parse(localStorage.getItem("user"));

console.log(savedUser);
```

এই pattern বুঝে ফেললে **localStorage + JSON + object/array persistence**-এর মূল concept তোমার পরিষ্কার হয়ে যাবে।

**Chapter 18 complete.**
