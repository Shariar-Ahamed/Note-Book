# 📘 Chapter 17 — Forms & Form Validation

### Forms & Form Validation — ফর্ম তৈরি, ডাটা নেওয়া এবং Validation করা

JavaScript-এর মাধ্যমে ওয়েবসাইটে user-এর কাছ থেকে **Name, Email, Password, Phone, Address, Gender, Course, File** ইত্যাদি data নেওয়া এবং সেই data সঠিক কি না যাচাই করা যায়।

আজকের chapter-এ আমরা **HTML Form + JavaScript Form Handling + Validation API + FormData**—সবকিছু ধাপে ধাপে শিখব।

---

# 17.1 Form কী?

**Form** হলো এমন একটি UI অংশ যেখানে user information input করে।

### Real-life example

তুমি কোনো website-এ registration করছো:

```text
Name:     Shariar
Email:    shariar@example.com
Password: ********
Phone:    017XXXXXXXX

          [ Register ]
```

এই পুরো অংশটাই একটি **Form**।

HTML:

```html
<form>
  <input type="text" />
  <input type="email" />
  <input type="password" />

  <button type="submit">Register</button>
</form>
```

---

# 17.2 Basic Form Structure

```html
<form id="registrationForm">
  <label for="name">Name</label>
  <input type="text" id="name" />

  <label for="email">Email</label>
  <input type="email" id="email" />

  <button type="submit">Register</button>
</form>
```

JavaScript:

```javascript
const form = document.querySelector("#registrationForm");

form.addEventListener("submit", function (event) {
  event.preventDefault();

  console.log("Form submitted!");
});
```

### Output

```text
Form submitted!
```

### এখানে কী হলো?

```javascript
form.addEventListener("submit", ...)
```

Form submit হলে function চলবে।

আর:

```javascript
event.preventDefault();
```

browser-এর default form submission বন্ধ করবে।

---

# 17.3 কেন `preventDefault()` ব্যবহার করি?

Normally form submit করলে browser page reload করতে পারে।

```javascript
form.addEventListener("submit", function (event) {
  event.preventDefault();
});
```

এতে browser-এর default behavior বন্ধ হয়।

### Real-life example

Login form submit করলে তুমি চাও:

```text
User clicks Login
       ↓
JavaScript data নেয়
       ↓
Validation করে
       ↓
API-তে data পাঠায়
       ↓
Login success
```

Page reload করানো দরকার নেই।

---

# 17.4 Input থেকে Value নেওয়া

```html
<input type="text" id="username" />

<button id="btn">Get Name</button>
```

```javascript
const username = document.querySelector("#username");
const button = document.querySelector("#btn");

button.addEventListener("click", function () {
  console.log(username.value);
});
```

User যদি লেখে:

```text
Shariar
```

Output:

```text
Shariar
```

### গুরুত্বপূর্ণ

Input-এর value পেতে:

```javascript
input.value;
```

---

# 17.5 Form-এর সব Input Access করা

```html
<form id="myForm">
  <input type="text" name="username" />

  <input type="email" name="email" />

  <input type="password" name="password" />

  <button type="submit">Submit</button>
</form>
```

JavaScript:

```javascript
const form = document.querySelector("#myForm");

console.log(form.elements);
```

`form.elements` form-এর controls-এর collection দেয়।

---

# 17.6 `form.elements` দিয়ে নির্দিষ্ট Input

```javascript
const form = document.querySelector("#myForm");

console.log(form.elements.username.value);
console.log(form.elements.email.value);
console.log(form.elements.password.value);
```

### Output

```text
Shariar
shariar@gmail.com
12345678
```

এখানে `name` attribute গুরুত্বপূর্ণ।

```html
<input name="username" />
```

---

# 17.7 Label কেন ব্যবহার করি?

ভালো form-এর জন্য:

```html
<label for="email">Email</label>

<input type="email" id="email" />
```

`for="email"` → input-এর `id="email"`-এর সাথে সম্পর্ক তৈরি করে।

User label-এ click করলেও input focus হবে।

---

# 17.8 Different Input Types

Form-এ বিভিন্ন ধরনের input আছে।

```html
<input type="text" />

<input type="email" />

<input type="password" />

<input type="number" />

<input type="tel" />

<input type="date" />

<input type="time" />

<input type="url" />

<input type="checkbox" />

<input type="radio" />

<input type="file" />

<input type="color" />

<input type="range" />
```

JavaScript দিয়ে এগুলোর value/behavior handle করা যায়।

---

# 17.9 `required`

User যেন field empty রেখে submit করতে না পারে:

```html
<input type="text" name="username" required />
```

User empty রেখে submit করলে browser validation message দেখাবে।

---

# 17.10 `minlength` এবং `maxlength`

```html
<input type="text" minlength="3" maxlength="20" />
```

এর মানে:

```text
Minimum → 3 characters
Maximum → 20 characters
```

---

# 17.11 `min` এবং `max`

Number-এর ক্ষেত্রে:

```html
<input type="number" min="18" max="60" />
```

Allowed range:

```text
18 → 60
```

---

# 17.12 `pattern`

Custom pattern তৈরি করা যায়।

```html
<input type="text" pattern="[A-Za-z]+" />
```

এখানে শুধুমাত্র English alphabet allow করা হচ্ছে।

---

# 17.13 `autocomplete`

Browser যেন আগের information suggest করতে পারে:

```html
<input type="email" autocomplete="email" />
```

Password:

```html
<input type="password" autocomplete="current-password" />
```

Registration-এর নতুন password:

```html
<input type="password" autocomplete="new-password" />
```

---

# 17.14 `novalidate`

Form-এর browser validation বন্ধ করতে:

```html
<form novalidate></form>
```

এটি ব্যবহার করলে validation JavaScript দিয়ে নিজে করতে পারো।

---

# 17.15 HTML Constraint Validation

HTML নিজেই অনেক validation করতে পারে।

```html
<form>
  <input type="email" required />

  <input type="number" min="18" max="60" />

  <button type="submit">Submit</button>
</form>
```

Browser automatically check করবে:

```text
required
email format
min
max
pattern
minlength
maxlength
```

---

# 17.16 `checkValidity()`

কোনো form বা input valid কিনা check করতে:

```javascript
const email = document.querySelector("#email");

console.log(email.checkValidity());
```

Valid হলে:

```text
true
```

Invalid হলে:

```text
false
```

---

# 17.17 Example

```html
<input id="email" type="email" required />
```

JavaScript:

```javascript
const email = document.querySelector("#email");

console.log(email.checkValidity());
```

Empty হলে:

```text
false
```

Valid email দিলে:

```text
true
```

---

# 17.18 `reportValidity()`

Browser-এর built-in validation message দেখাতে:

```javascript
email.reportValidity();
```

যদি email invalid হয় browser message দেখাবে।

---

# 17.19 `validity` Object

JavaScript-এর সবচেয়ে গুরুত্বপূর্ণ form validation concepts-এর একটি।

```javascript
console.log(email.validity);
```

এখানে অনেক property পাওয়া যায়।

যেমন:

```javascript
email.validity.valid;
```

---

# 17.20 Important Validity Properties

| Property          | Meaning                           |
| ----------------- | --------------------------------- |
| `valueMissing`    | required field empty              |
| `typeMismatch`    | email/url type ভুল                |
| `patternMismatch` | pattern match করেনি               |
| `tooLong`         | maximum length ছাড়িয়েছে           |
| `tooShort`        | minimum length-এর নিচে            |
| `rangeUnderflow`  | min-এর নিচে                       |
| `rangeOverflow`   | max-এর উপরে                       |
| `stepMismatch`    | step-এর সাথে match করছে না        |
| `badInput`        | browser input parse করতে পারছে না |
| `customError`     | custom validation error           |
| `valid`           | সব validation ঠিক                 |

---

# 17.21 Example

```html
<input id="email" type="email" required />
```

JavaScript:

```javascript
const email = document.querySelector("#email");

console.log(email.validity.valueMissing);
console.log(email.validity.typeMismatch);
console.log(email.validity.valid);
```

Empty থাকলে:

```text
true
false
false
```

---

# 17.22 `validationMessage`

Browser-এর validation message পাওয়া যায়:

```javascript
console.log(email.validationMessage);
```

যেমন:

```text
Please fill out this field.
```

অথবা email ভুল হলে browser অনুযায়ী message আসতে পারে।

---

# 17.23 `setCustomValidity()`

নিজের custom error message তৈরি করতে পারো।

```javascript
const username = document.querySelector("#username");

if (username.value.length < 5) {
  username.setCustomValidity("Username must be at least 5 characters.");
} else {
  username.setCustomValidity("");
}
```

### গুরুত্বপূর্ণ

Error remove করতে:

```javascript
setCustomValidity("");
```

---

# 17.24 Complete Custom Validation

```html
<input id="username" type="text" />

<button id="check">Check</button>
```

```javascript
const username = document.querySelector("#username");
const button = document.querySelector("#check");

button.addEventListener("click", function () {
  if (username.value.trim().length < 5) {
    username.setCustomValidity("Username must be at least 5 characters.");

    username.reportValidity();
  } else {
    username.setCustomValidity("");

    console.log("Valid username");
  }
});
```

---

# 17.25 `trim()` কেন ব্যবহার করি?

User accidentally space দিতে পারে।

```javascript
const name = "   Shariar   ";

console.log(name.trim());
```

Output:

```text
Shariar
```

Form validation-এ:

```javascript
const name = input.value.trim();
```

এটি খুব useful।

---

# 17.26 Basic Name Validation

```html
<input id="name" type="text" />

<button id="submitBtn">Submit</button>
```

```javascript
const nameInput = document.querySelector("#name");
const submitBtn = document.querySelector("#submitBtn");

submitBtn.addEventListener("click", function () {
  const name = nameInput.value.trim();

  if (name === "") {
    console.log("Name is required.");
  } else {
    console.log("Name:", name);
  }
});
```

---

# 17.27 Email Validation

HTML:

```html
<input id="email" type="email" />
```

JavaScript:

```javascript
const email = document.querySelector("#email");

if (!email.checkValidity()) {
  console.log("Invalid email");
} else {
  console.log("Valid email");
}
```

---

# 17.28 Password Validation

ধরি password minimum 8 characters হতে হবে।

```javascript
const password = "123456";

if (password.length < 8) {
  console.log("Password must contain at least 8 characters.");
} else {
  console.log("Password is valid.");
}
```

Output:

```text
Password must contain at least 8 characters.
```

---

# 17.29 Confirm Password

Real-life registration form-এ:

```text
Password:
Confirm Password:
```

JavaScript:

```javascript
const password = "Shariar123";
const confirmPassword = "Shariar123";

if (password !== confirmPassword) {
  console.log("Passwords do not match.");
} else {
  console.log("Password matched.");
}
```

Output:

```text
Password matched.
```

---

# 17.30 Phone Number Validation

ধরি Bangladesh-এর 11-digit phone number check করব।

```javascript
const phone = "01712345678";

const pattern = /^01[3-9]\d{8}$/;

console.log(pattern.test(phone));
```

Output:

```text
true
```

ভুল হলে:

```javascript
const phone = "123456";

console.log(pattern.test(phone));
```

Output:

```text
false
```

---

# 17.31 Regex দিয়ে Email Validation

```javascript
const emailPattern = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;

const email = "shariar@gmail.com";

console.log(emailPattern.test(email));
```

Output:

```text
true
```

⚠️ তবে production application-এ email validation-এর জন্য শুধু regex-এর উপর নির্ভর করা উচিত নয়।

---

# 17.32 Form Submit Handling

এখন একটা complete example দেখি।

### HTML

```html
<form id="signupForm">
  <label>Name</label>
  <input type="text" id="name" required />

  <label>Email</label>
  <input type="email" id="email" required />

  <label>Password</label>
  <input type="password" id="password" minlength="8" required />

  <button type="submit">Sign Up</button>
</form>
```

### JavaScript

```javascript
const form = document.querySelector("#signupForm");

form.addEventListener("submit", function (event) {
  event.preventDefault();

  if (!form.checkValidity()) {
    form.reportValidity();
    return;
  }

  const name = document.querySelector("#name").value.trim();

  const email = document.querySelector("#email").value.trim();

  console.log("Name:", name);
  console.log("Email:", email);
});
```

### Flow

```text
Submit
   ↓
preventDefault()
   ↓
checkValidity()
   ↓
Invalid?
 ├── Yes → reportValidity()
 └── No
       ↓
Read values
       ↓
Process / API
```

---

# 17.33 `FormData`

Form-এর data একসাথে collect করার জন্য `FormData` খুব useful।

HTML:

```html
<form id="loginForm">
  <input type="email" name="email" />

  <input type="password" name="password" />

  <button type="submit">Login</button>
</form>
```

JavaScript:

```javascript
const form = document.querySelector("#loginForm");

form.addEventListener("submit", function (event) {
  event.preventDefault();

  const formData = new FormData(form);

  console.log(formData.get("email"));
  console.log(formData.get("password"));
});
```

---

# 17.34 `FormData.get()`

```javascript
formData.get("email");
```

একটি field-এর value দেয়।

---

# 17.35 `FormData.has()`

```javascript
console.log(formData.has("email"));
```

Output:

```text
true
```

---

# 17.36 `FormData.set()`

```javascript
formData.set("email", "newemail@gmail.com");
```

Existing value replace করবে।

---

# 17.37 `FormData.append()`

```javascript
formData.append("role", "student");
```

নতুন data যোগ করবে।

---

# 17.38 `FormData.delete()`

```javascript
formData.delete("password");
```

Field remove করবে।

---

# 17.39 FormData-এর সব Data দেখা

```javascript
for (const [key, value] of formData.entries()) {
  console.log(key, value);
}
```

Output:

```text
email shariar@gmail.com
password 12345678
```

---

# 17.40 FormData → Object

অনেক সময় API-তে JSON পাঠানোর জন্য object দরকার হয়।

```javascript
const data = Object.fromEntries(formData);

console.log(data);
```

Output:

```javascript
{
    email: "shariar@gmail.com",
    password: "12345678"
}
```

এটি খুব useful pattern।

---

# 17.41 Checkbox Handle করা

HTML:

```html
<label>
  <input type="checkbox" id="terms" />

  I agree to Terms
</label>
```

JavaScript:

```javascript
const terms = document.querySelector("#terms");

console.log(terms.checked);
```

Checked হলে:

```text
true
```

Unchecked হলে:

```text
false
```

---

# 17.42 Checkbox Validation

```javascript
if (!terms.checked) {
  console.log("You must accept the terms.");
}
```

---

# 17.43 Radio Button

```html
<label>
  <input type="radio" name="gender" value="male" />
  Male
</label>

<label>
  <input type="radio" name="gender" value="female" />
  Female
</label>
```

JavaScript:

```javascript
const selected = document.querySelector('input[name="gender"]:checked');

console.log(selected?.value);
```

Male select করলে:

```text
male
```

---

# 17.44 Select Dropdown

HTML:

```html
<select id="course">
  <option value="">Select Course</option>

  <option value="cse">CSE</option>

  <option value="eee">EEE</option>

  <option value="bba">BBA</option>
</select>
```

JavaScript:

```javascript
const course = document.querySelector("#course");

console.log(course.value);
```

---

# 17.45 Select Validation

```javascript
if (course.value === "") {
  console.log("Please select a course.");
}
```

---

# 17.46 File Input

HTML:

```html
<input type="file" id="profileImage" />
```

JavaScript:

```javascript
const fileInput = document.querySelector("#profileImage");

console.log(fileInput.files);
```

Selected files:

```javascript
console.log(fileInput.files[0]);
```

File-এর নাম:

```javascript
console.log(fileInput.files[0].name);
```

---

# 17.47 File Size Check

```javascript
const file = fileInput.files[0];

if (file) {
  const sizeInMB = file.size / (1024 * 1024);

  if (sizeInMB > 2) {
    console.log("File must be under 2MB.");
  }
}
```

---

# 17.48 Form Reset

Form-এর সব field reset করতে:

```javascript
form.reset();
```

Example:

```javascript
form.addEventListener("submit", function (event) {
  event.preventDefault();

  console.log("Submitted");

  form.reset();
});
```

---

# 17.49 Disable Submit Button

Duplicate submission prevent করতে button disable করা যায়।

```javascript
const submitButton = form.querySelector('button[type="submit"]');

form.addEventListener("submit", function (event) {
  event.preventDefault();

  submitButton.disabled = true;

  console.log("Processing...");
});
```

Real-world application-এ API request চলাকালে এটি useful।

---

# 17.50 Complete Registration Form Project

এবার অনেকগুলো concept একসাথে ব্যবহার করি।

### HTML

```html
<form id="registerForm">
  <h2>Create Account</h2>

  <label for="name"> Name </label>

  <input id="name" name="name" type="text" minlength="3" required />

  <br /><br />

  <label for="email"> Email </label>

  <input id="email" name="email" type="email" required />

  <br /><br />

  <label for="password"> Password </label>

  <input id="password" name="password" type="password" minlength="8" required />

  <br /><br />

  <label for="confirmPassword"> Confirm Password </label>

  <input id="confirmPassword" type="password" required />

  <br /><br />

  <label>
    <input id="terms" type="checkbox" />

    I agree to the terms
  </label>

  <br /><br />

  <button type="submit">Register</button>
</form>
```

### JavaScript

```javascript
const form = document.querySelector("#registerForm");

const password = document.querySelector("#password");

const confirmPassword = document.querySelector("#confirmPassword");

const terms = document.querySelector("#terms");

form.addEventListener("submit", function (event) {
  event.preventDefault();

  // HTML validation
  if (!form.checkValidity()) {
    form.reportValidity();

    return;
  }

  // Password validation
  if (password.value !== confirmPassword.value) {
    confirmPassword.setCustomValidity("Passwords do not match.");

    confirmPassword.reportValidity();

    return;
  } else {
    confirmPassword.setCustomValidity("");
  }

  // Terms validation
  if (!terms.checked) {
    alert("You must accept the terms.");

    return;
  }

  // Get form data
  const formData = new FormData(form);

  const data = Object.fromEntries(formData);

  console.log(data);

  alert("Registration successful!");
});
```

---

# 17.51 এই Project-এর পুরো Flow

```text
User fills form
       ↓
Submit
       ↓
preventDefault()
       ↓
HTML validation
       ↓
checkValidity()
       ↓
Password match?
       ↓
Terms accepted?
       ↓
FormData
       ↓
Object.fromEntries()
       ↓
API / Backend
```

এটাই real-world registration form-এর basic architecture।

---

# 17.52 Client-side Validation vs Server-side Validation

এটা **খুব গুরুত্বপূর্ণ**।

Client-side:

```javascript
if (password.length < 8) {
  // error
}
```

এটি user experience-এর জন্য ভালো।

কিন্তু security-এর জন্য যথেষ্ট নয়।

কারণ user browser-এর JavaScript modify/disable করতে পারে।

তাই:

```text
Frontend Validation
        +
Backend Validation
        =
Proper Validation
```

Backend-এ আবার validate করতে হবে:

```text
Email
Password
Phone
Required fields
Data type
Authorization
Business rules
```

---

# 17.53 Example: Frontend + Backend

Frontend:

```javascript
const data = {
  email: "user@gmail.com",
  password: "12345678",
};
```

API request:

```text
POST /api/register
```

Backend:

```text
Receive data
    ↓
Validate
    ↓
Sanitize/process
    ↓
Check existing user
    ↓
Hash password
    ↓
Save database
```

Frontend validation কখনোই backend validation-এর replacement নয়।

---

# 17.54 `aria-invalid`

Accessibility-এর জন্য invalid field indicate করা যায়।

```javascript
email.setAttribute("aria-invalid", "true");
```

Valid হলে:

```javascript
email.setAttribute("aria-invalid", "false");
```

---

# 17.55 Error Message দেখানোর Better Way

শুধু:

```javascript
alert("Invalid email");
```

এর পরিবর্তে UI-তে message দেখানো ভালো।

HTML:

```html
<input id="email" type="email" />

<p id="emailError"></p>
```

JavaScript:

```javascript
const email = document.querySelector("#email");

const error = document.querySelector("#emailError");

if (!email.checkValidity()) {
  error.textContent = "Please enter a valid email.";
} else {
  error.textContent = "";
}
```

এতে user সরাসরি field-এর নিচে error দেখতে পাবে।

---

# 17.56 `input` Event দিয়ে Live Validation

User typing করার সময় validation করতে পারি।

```javascript
email.addEventListener("input", function () {
  if (email.validity.typeMismatch) {
    console.log("Invalid email");
  } else {
    console.log("Valid email");
  }
});
```

Real-life:

```text
user@gmail
       ↓
Invalid

user@gmail.com
       ↓
Valid
```

---

# 17.57 Password Strength Checker

```javascript
const passwordInput = document.querySelector("#password");

passwordInput.addEventListener("input", function () {
  const password = passwordInput.value;

  if (password.length < 8) {
    console.log("Weak");
  } else if (password.length < 12) {
    console.log("Medium");
  } else {
    console.log("Strong");
  }
});
```

---

# 17.58 Form Validation-এর Common Mistakes

### ❌ Mistake 1

শুধু frontend validation-এর উপর depend করা।

### ❌ Mistake 2

Password plaintext database-এ save করা।

### ❌ Mistake 3

User input blindly `innerHTML`-এ বসানো।

### ❌ Mistake 4

`trim()` না করা।

### ❌ Mistake 5

Duplicate form submission allow করা।

### ❌ Mistake 6

শুধু regex দিয়ে সব validation করার চেষ্টা করা।

---

# 🔥 17.59 Must Know

এই chapter থেকে অবশ্যই জানতে হবে:

```text
<form>
```

```text
submit event
```

```javascript
event.preventDefault();
```

```javascript
input.value;
```

```javascript
form.elements;
```

```javascript
required;
```

```javascript
minlength;
maxlength;
```

```javascript
min;
max;
```

```javascript
pattern;
```

```javascript
checkValidity();
```

```javascript
reportValidity();
```

```javascript
validity;
```

```javascript
validationMessage;
```

```javascript
setCustomValidity();
```

```javascript
FormData;
```

```javascript
formData.get();
```

```javascript
formData.set();
```

```javascript
formData.append();
```

```javascript
formData.delete();
```

```javascript
Object.fromEntries();
```

```javascript
form.reset();
```

```javascript
input.checked;
```

```javascript
input.files;
```

---

# 🧠 17.60 Quick Cheat Sheet

| কাজ                 | JavaScript               |
| ------------------- | ------------------------ |
| Input value         | `input.value`            |
| Checkbox            | `input.checked`          |
| File                | `input.files`            |
| Form elements       | `form.elements`          |
| Submit event        | `"submit"`               |
| Default submit বন্ধ | `event.preventDefault()` |
| Valid কিনা          | `checkValidity()`        |
| Browser message     | `reportValidity()`       |
| Validation info     | `input.validity`         |
| Error message       | `validationMessage`      |
| Custom error        | `setCustomValidity()`    |
| Form data           | `new FormData(form)`     |
| Data নেওয়া          | `formData.get()`         |
| Data যোগ            | `formData.append()`      |
| Data replace        | `formData.set()`         |
| Data delete         | `formData.delete()`      |
| সব data             | `formData.entries()`     |
| Form reset          | `form.reset()`           |

---

# 📝 17.61 Practice Set

### Practice 1 — Login Form

তৈরি করো:

```text
Email
Password
Login Button
```

Validation:

```text
Email required
Valid email
Password minimum 8 characters
```

---

### Practice 2 — Registration Form

Fields:

```text
Name
Email
Phone
Password
Confirm Password
Gender
Course
Terms
```

সব validation implement করো।

---

### Practice 3 — Password Strength

```text
< 8       → Weak
8–11      → Medium
12+       → Strong
```

---

### Practice 4 — Contact Form

Fields:

```text
Name
Email
Subject
Message
```

Rules:

```text
Name → required
Email → valid email
Subject → minimum 5 characters
Message → minimum 20 characters
```

---

### Practice 5 — Student Registration

```text
Student Name
Student ID
Email
Department
Semester
Phone
```

সব data `FormData` দিয়ে collect করে:

```javascript
Object.fromEntries(formData);
```

এর মাধ্যমে object তৈরি করো।

---

# 🎯 Chapter 17 Final Mental Map

```text
                  FORM
                   │
          ┌────────┴────────┐
          ↓                 ↓
       INPUTS             BUTTON
          │                 │
          ↓                 ↓
       value             submit
          │                 │
          └────────┬────────┘
                   ↓
          preventDefault()
                   ↓
            VALIDATION
                   │
       ┌───────────┼───────────┐
       ↓           ↓           ↓
    required     pattern    custom JS
       │           │           │
       └───────────┼───────────┘
                   ↓
            checkValidity()
                   ↓
               FormData
                   ↓
          Object.fromEntries()
                   ↓
             API / Backend
                   ↓
               Database
```

### ⭐ সবচেয়ে গুরুত্বপূর্ণ কথা

**Form Handling** হলো frontend development-এর অত্যন্ত গুরুত্বপূর্ণ একটি অংশ। তুমি যখন React/Next.js দিয়ে **Login, Registration, Contact Form, Dashboard, Admin Panel, Checkout, Student Portal** বানাবে—এই JavaScript fundamentals-গুলোই ভিতরে কাজ করবে।

**Chapter 17 complete.**
