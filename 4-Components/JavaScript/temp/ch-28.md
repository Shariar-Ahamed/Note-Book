# 📘 Chapter 28 — Symbols, Proxy & Reflect

এখন আমরা JavaScript-এর আরও একটি **Advanced Core Chapter**-এ ঢুকছি।

এই Chapter-এর তিনটি প্রধান বিষয়:

```text
Symbol
Proxy
Reflect
```

প্রথমে এগুলো আলাদা মনে হবে, কিন্তু শেষে দেখবে—এগুলো JavaScript-এর **object behavior, metaprogramming, custom behavior এবং framework-level programming** বুঝতে অনেক গুরুত্বপূর্ণ।

---

# 🎯 Chapter 28 Learning Goals

এই Chapter শেষে তুমি জানতে পারবে:

* `Symbol` কী
* কেন Symbol দরকার
* Unique property keys
* `Symbol()` বনাম `Symbol.for()`
* Global Symbol Registry
* Well-known Symbols
* `Symbol.iterator`
* Custom iterable
* Symbol দিয়ে hidden-like properties
* `Proxy` কী
* Proxy-এর `get`, `set`, `has`, `deleteProperty`
* `apply`, `construct`
* Validation
* Access control
* Logging
* Reactive behavior
* `Reflect`
* Proxy + Reflect
* Metaprogramming
* Common mistakes
* Real-life use cases

---

# 28.1 — Symbol কী?

`Symbol` হলো JavaScript-এর একটি **primitive data type**।

JavaScript-এর primitive types:

```text
String
Number
BigInt
Boolean
Undefined
Null
Symbol
```

Symbol তৈরি করা যায়:

```javascript
const id = Symbol();
```

---

# 28.2 — Symbol কেন দরকার?

ধরো দুটি property একই নামের:

```javascript
const user = {
    id: 101
};

user.id = 202;
```

আগের value replace হয়ে যাবে।

কিন্তু Symbol ব্যবহার করলে unique key তৈরি করা যায়।

```javascript
const id1 = Symbol();
const id2 = Symbol();

console.log(id1 === id2);
```

Output:

```text
false
```

প্রতিটি Symbol সাধারণত unique।

---

# 28.3 — Symbol-এর Real-Life Idea

ধরো একটি বড় application-এ অনেক developer কাজ করছে।

একজন লিখল:

```javascript
user.status
```

আরেকজনও লিখল:

```javascript
user.status
```

Property collision হতে পারে।

Symbol:

```javascript
const internalStatus = Symbol();

user[internalStatus] = "active";
```

এতে একটি unique key তৈরি হলো।

---

# 28.4 — Symbol-এর Type

```javascript
const id = Symbol();

console.log(typeof id);
```

Output:

```text
symbol
```

---

# 28.5 — Symbol Description

Symbol তৈরি করার সময় description দেওয়া যায়।

```javascript
const id = Symbol("userId");

console.log(id);
```

Output:

```text
Symbol(userId)
```

Description মূলত debugging-এর জন্য helpful।

---

# 28.6 — Description Access

```javascript
const id = Symbol("userId");

console.log(id.description);
```

Output:

```text
userId
```

---

# 28.7 — Symbol Always Unique

```javascript
const a = Symbol("id");
const b = Symbol("id");

console.log(a === b);
```

Output:

```text
false
```

যদিও description একই:

```text
id
```

তবুও Symbol দুটো আলাদা।

---

# 28.8 — Symbol as Object Property

```javascript
const userId = Symbol("userId");

const user = {
    name: "Shariar",
    [userId]: 101
};

console.log(user.name);
console.log(user[userId]);
```

Output:

```text
Shariar
101
```

খেয়াল করো:

```javascript
[userId]
```

computed property syntax ব্যবহার করা হয়েছে।

---

# 28.9 — Dot Notation দিয়ে Symbol Access করা যায় না

এটা ভুল:

```javascript
user.userId
```

কারণ `userId` এখানে Symbol variable নয়।

সঠিক:

```javascript
user[userId]
```

---

# 28.10 — Symbol Property কি Hidden?

Symbol property **private নয়**।

এটা খুব গুরুত্বপূর্ণ।

```javascript
const secret = Symbol("secret");

const user = {
    name: "Shariar",
    [secret]: "Hello"
};
```

`Object.keys()` করলে:

```javascript
console.log(Object.keys(user));
```

Output:

```text
["name"]
```

Symbol key এখানে আসে না।

কিন্তু:

```javascript
console.log(Object.getOwnPropertySymbols(user));
```

Output:

```text
[Symbol(secret)]
```

অর্থাৎ:

> Symbol property hidden from some normal enumeration, কিন্তু private/security mechanism নয়।

---

# 28.11 — `Reflect.ownKeys()`

String এবং Symbol—দুই ধরনের own keys পেতে:

```javascript
const id = Symbol("id");

const user = {
    name: "Shariar",
    [id]: 101
};

console.log(Reflect.ownKeys(user));
```

Output-এর ধারণা:

```text
["name", Symbol(id)]
```

---

# 28.12 — Symbol বনাম String Key

| String Key            | Symbol Key                                                         |
| --------------------- | ------------------------------------------------------------------ |
| `"id"`                | `Symbol("id")`                                                     |
| collision হতে পারে    | unique                                                             |
| `Object.keys()`-এ আসে | `Object.keys()`-এ আসে না                                           |
| সাধারণ property       | special property key                                               |
| সাধারণভাবে enumerable | Symbol property-ও enumerable হতে পারে, কিন্তু Object.keys-এ আসে না |

---

# 28.13 — `Symbol.for()`

এখন একটি গুরুত্বপূর্ণ বিষয়।

```javascript
const a = Symbol("user");
const b = Symbol("user");

console.log(a === b);
```

Output:

```text
false
```

কিন্তু:

```javascript
const a = Symbol.for("user");
const b = Symbol.for("user");

console.log(a === b);
```

Output:

```text
true
```

কারণ `Symbol.for()` **Global Symbol Registry** ব্যবহার করে।

---

# 28.14 — `Symbol.for()` কীভাবে কাজ করে?

```javascript
Symbol.for("app.user")
```

প্রথমবার তৈরি হলে registry-তে রাখে।

আবার:

```javascript
Symbol.for("app.user")
```

করলে একই Symbol পাওয়া যায়।

Mental model:

```text
Symbol("user")
   ↓
নতুন unique Symbol

Symbol.for("user")
   ↓
Global Registry
   ↓
একই key হলে একই Symbol
```

---

# 28.15 — `Symbol.keyFor()`

`Symbol.for()` দিয়ে তৈরি Symbol-এর key বের করতে:

```javascript
const userSymbol = Symbol.for("user");

console.log(Symbol.keyFor(userSymbol));
```

Output:

```text
user
```

---

# 28.16 — `Symbol.for()` বনাম `Symbol()`

| `Symbol()`                   | `Symbol.for()`            |
| ---------------------------- | ------------------------- |
| নতুন Symbol তৈরি করে         | Registry থেকে Symbol নেয়  |
| প্রতিবার unique              | একই key → একই Symbol      |
| Global registry নয়           | Global Symbol Registry    |
| `Symbol.keyFor()` কাজ করে না | `Symbol.keyFor()` কাজ করে |

---

# 28.17 — Well-Known Symbols

JavaScript কিছু built-in Symbol দিয়েছে যেগুলো language behavior customize করতে সাহায্য করে।

Examples:

```text
Symbol.iterator
Symbol.toStringTag
Symbol.toPrimitive
Symbol.toStringTag
Symbol.hasInstance
Symbol.asyncIterator
Symbol.species
Symbol.isConcatSpreadable
```

সবচেয়ে গুরুত্বপূর্ণ কয়েকটি আমরা দেখব।

---

# 28.18 — `Symbol.iterator`

Chapter 27-এ আমরা এটা দেখেছি।

এটি কোনো object-এর iteration behavior define করতে পারে।

```javascript
const numbers = {

    *[Symbol.iterator]() {

        yield 10;
        yield 20;
        yield 30;

    }

};

for (const number of numbers) {
    console.log(number);
}
```

Output:

```text
10
20
30
```

এখানে:

```javascript
Symbol.iterator
```

object-টাকে iterable করেছে।

---

# 28.19 — `Symbol.toStringTag`

Object-এর `Object.prototype.toString()` result customize করা যায়।

```javascript
const user = {

    [Symbol.toStringTag]: "User"

};

console.log(Object.prototype.toString.call(user));
```

Output:

```text
[object User]
```

---

# 28.20 — `Symbol.toPrimitive`

Object primitive value-তে convert হওয়ার সময় behavior customize করা যায়।

```javascript
const money = {

    value: 100,

    [Symbol.toPrimitive](hint) {

        if (hint === "number") {
            return this.value;
        }

        return `$${this.value}`;
    }
};

console.log(+money);
console.log(`${money}`);
```

Output:

```text
100
$100
```

এটি advanced metaprogramming-এর example।

---

# 28.21 — `Symbol.hasInstance`

`instanceof` behavior customize করা যায়।

```javascript
class EvenNumber {

    static [Symbol.hasInstance](value) {

        return typeof value === "number" && value % 2 === 0;

    }

}

console.log(2 instanceof EvenNumber);
console.log(3 instanceof EvenNumber);
```

Output:

```text
true
false
```

এখানে `instanceof`-এর behavior custom করা হয়েছে।

---

# 🔥 Symbol-এর সবচেয়ে গুরুত্বপূর্ণ ব্যবহার

Practical JavaScript-এ তোমার জন্য সবচেয়ে গুরুত্বপূর্ণ:

```text
1. Unique property keys
2. Avoid property collisions
3. Symbol.iterator
4. Well-known Symbols
5. Metaprogramming
```

---

# 28.22 — এখন আসি Proxy-তে

`Proxy` হলো JavaScript-এর একটি powerful feature যেটা কোনো object-এর operations **intercept এবং customize** করতে পারে।

সহজ ভাষায়:

> Proxy হলো object-এর সামনে একটি "middle layer"।

Mental model:

```text
Code
  ↓
Proxy
  ↓
Target Object
```

---

# 28.23 — Basic Proxy

```javascript
const user = {
    name: "Shariar"
};

const proxy = new Proxy(user, {});

console.log(proxy.name);
```

Output:

```text
Shariar
```

এখানে:

```text
user = target
proxy = wrapper
```

---

# 28.24 — Proxy Handler

Proxy তৈরি:

```javascript
new Proxy(target, handler)
```

Example:

```javascript
const user = {
    name: "Shariar"
};

const handler = {

    get(target, property) {

        console.log(`Reading: ${property}`);

        return target[property];
    }

};

const proxy = new Proxy(user, handler);

console.log(proxy.name);
```

Output:

```text
Reading: name
Shariar
```

---

# 28.25 — `get` Trap

`get` trap property access intercept করে।

```javascript
const user = {
    name: "Shariar",
    age: 23
};

const proxy = new Proxy(user, {

    get(target, property) {

        console.log("Accessing:", property);

        return target[property];
    }

});

console.log(proxy.name);
console.log(proxy.age);
```

Output:

```text
Accessing: name
Shariar

Accessing: age
23
```

---

# 28.26 — `set` Trap

Property set করার সময় intercept করা যায়।

```javascript
const user = {
    name: "Shariar"
};

const proxy = new Proxy(user, {

    set(target, property, value) {

        console.log(`Setting ${property} = ${value}`);

        target[property] = value;

        return true;
    }

});

proxy.name = "Ripon";
```

Output:

```text
Setting name = Ripon
```

---

# 28.27 — Proxy দিয়ে Validation

এটি খুব practical।

```javascript
const user = {

    name: "Shariar",
    age: 23

};

const proxy = new Proxy(user, {

    set(target, property, value) {

        if (property === "age") {

            if (typeof value !== "number") {
                throw new TypeError("Age must be a number");
            }

            if (value < 0) {
                throw new RangeError("Age cannot be negative");
            }
        }

        target[property] = value;

        return true;
    }

});

proxy.age = 25;

console.log(proxy.age);
```

Output:

```text
25
```

কিন্তু:

```javascript
proxy.age = -10;
```

Error:

```text
RangeError: Age cannot be negative
```

---

# 28.28 — `has` Trap

`in` operator intercept করা যায়।

```javascript
const user = {
    name: "Shariar"
};

const proxy = new Proxy(user, {

    has(target, property) {

        console.log(`Checking: ${property}`);

        return property in target;
    }

});

console.log("name" in proxy);
```

Output:

```text
Checking: name
true
```

---

# 28.29 — `deleteProperty` Trap

`delete` operation intercept করা যায়।

```javascript
const user = {
    name: "Shariar",
    age: 23
};

const proxy = new Proxy(user, {

    deleteProperty(target, property) {

        console.log(`Deleting: ${property}`);

        return delete target[property];
    }

});

delete proxy.age;

console.log(user);
```

---

# 28.30 — `ownKeys` Trap

`Object.keys()`/reflection-related key retrieval customize করা যায়।

```javascript
const user = {
    name: "Shariar",
    age: 23
};

const proxy = new Proxy(user, {

    ownKeys(target) {

        return ["name"];
    }

});

console.log(Object.keys(proxy));
```

Output:

```text
["name"]
```

এখানে `age` hide করা হয়েছে enumeration থেকে।

---

# 28.31 — Proxy Logging System

Proxy দিয়ে property access log করা যায়।

```javascript
const user = {
    name: "Shariar",
    age: 23,
    city: "Dhaka"
};

const loggedUser = new Proxy(user, {

    get(target, property) {

        console.log(
            `GET → ${String(property)}`
        );

        return target[property];
    },

    set(target, property, value) {

        console.log(
            `SET → ${String(property)} = ${value}`
        );

        target[property] = value;

        return true;
    }

});

console.log(loggedUser.name);

loggedUser.age = 24;
```

এটি debugging বা development tooling-এ useful হতে পারে।

---

# 28.32 — `apply` Trap

Proxy শুধু object-এর property operation intercept করে না।

Function-এর invocation-ও intercept করতে পারে।

```javascript
function greet(name) {
    return `Hello ${name}`;
}

const proxy = new Proxy(greet, {

    apply(target, thisArg, args) {

        console.log("Function called");

        return target(...args);
    }

});

console.log(proxy("Shariar"));
```

Output:

```text
Function called
Hello Shariar
```

---

# 28.33 — `construct` Trap

`new` operation intercept করা যায়।

```javascript
class User {

    constructor(name) {
        this.name = name;
    }

}

const ProxyUser = new Proxy(User, {

    construct(target, args) {

        console.log("Creating User");

        return new target(...args);
    }

});

const user = new ProxyUser("Shariar");
```

Output:

```text
Creating User
```

---

# 28.34 — Proxy Trap Summary

সব trap মুখস্থ করার দরকার নেই।

Important:

| Trap             | Intercepts     |
| ---------------- | -------------- |
| `get`            | property read  |
| `set`            | property write |
| `has`            | `in`           |
| `deleteProperty` | `delete`       |
| `ownKeys`        | keys           |
| `apply`          | function call  |
| `construct`      | `new`          |

---

# 28.35 — Proxy + Private Fields

এখানে একটি গুরুত্বপূর্ণ limitation আছে।

Private class fields:

```javascript
#password
```

Proxy দিয়ে সাধারণ property-এর মতো intercept করা যায় না।

```javascript
class User {

    #password = "12345";

}
```

Private field-এর access language-level private mechanism দ্বারা controlled।

তাই Proxy-কে security boundary হিসেবে ব্যবহার করা উচিত নয়।

---

# 28.36 — Proxy Revocable

একটি Proxy temporarily disable/revoke করা যায়।

```javascript
const user = {
    name: "Shariar"
};

const { proxy, revoke } = Proxy.revocable(user, {});

console.log(proxy.name);

revoke();

console.log(proxy.name);
```

শেষ access-এর সময় error হবে।

---

# 28.37 — এখন আসি Reflect-এ

`Reflect` হলো JavaScript-এর built-in object যেটা অনেক **object operation-এর standard methods** দেয়।

Examples:

```javascript
Reflect.get()
Reflect.set()
Reflect.has()
Reflect.deleteProperty()
Reflect.ownKeys()
Reflect.construct()
Reflect.apply()
```

---

# 28.38 — `Reflect.get()`

সাধারণ:

```javascript
user.name
```

Reflect:

```javascript
Reflect.get(user, "name");
```

Example:

```javascript
const user = {
    name: "Shariar"
};

console.log(
    Reflect.get(user, "name")
);
```

Output:

```text
Shariar
```

---

# 28.39 — `Reflect.set()`

```javascript
const user = {};

Reflect.set(user, "name", "Shariar");

console.log(user.name);
```

Output:

```text
Shariar
```

---

# 28.40 — `Reflect.has()`

```javascript
const user = {
    name: "Shariar"
};

console.log(
    Reflect.has(user, "name")
);
```

Output:

```text
true
```

এটি conceptually:

```javascript
"name" in user
```

এর মতো।

---

# 28.41 — `Reflect.deleteProperty()`

```javascript
const user = {
    name: "Shariar",
    age: 23
};

Reflect.deleteProperty(user, "age");

console.log(user);
```

---

# 28.42 — `Reflect.ownKeys()`

String + Symbol keys:

```javascript
const id = Symbol("id");

const user = {
    name: "Shariar",
    [id]: 101
};

console.log(
    Reflect.ownKeys(user)
);
```

এটি দুই ধরনের key দিতে পারে:

```text
"name"
Symbol(id)
```

---

# 28.43 — Reflect কেন Proxy-এর সাথে বেশি দেখা যায়?

এটি খুব গুরুত্বপূর্ণ।

Proxy handler-এ manually:

```javascript
target[property] = value;
```

করার পরিবর্তে:

```javascript
Reflect.set(target, property, value);
```

ব্যবহার করা অনেক ক্ষেত্রে safer এবং cleaner।

---

# 28.44 — Proxy + Reflect

Recommended pattern:

```javascript
const user = {
    name: "Shariar"
};

const proxy = new Proxy(user, {

    get(target, property, receiver) {

        console.log(`GET: ${String(property)}`);

        return Reflect.get(
            target,
            property,
            receiver
        );
    },

    set(target, property, value, receiver) {

        console.log(
            `SET: ${String(property)} = ${value}`
        );

        return Reflect.set(
            target,
            property,
            value,
            receiver
        );
    }

});

console.log(proxy.name);

proxy.name = "Ripon";
```

---

# 🧠 Proxy + Reflect Mental Model

```text
Code
 ↓
Proxy
 ↓
Trap
 ↓
Reflect
 ↓
Target Object
```

অর্থাৎ:

```text
Proxy = intercept
Reflect = perform standard operation
```

---

# 28.45 — Metaprogramming কী?

**Metaprogramming** মানে এমন code লেখা যা program-এর behavior/structure-এর ওপর কাজ করতে পারে বা customize করতে পারে।

JavaScript-এ:

```text
Proxy
Reflect
Symbols
Property Descriptors
```

এসব metaprogramming-এর tools।

---

# 28.46 — Real-Life Example: Read-Only Object

Proxy দিয়ে property পরিবর্তন block করা যায়।

```javascript
const settings = {

    theme: "dark",
    language: "en"

};

const readOnlySettings = new Proxy(settings, {

    set() {

        throw new Error(
            "Settings are read-only"
        );

    }

});

console.log(readOnlySettings.theme);

readOnlySettings.theme = "light";
```

শেষ line-এ error হবে।

---

# 28.47 — Real-Life Example: Access Control

```javascript
const user = {

    name: "Shariar",
    role: "user",
    salary: 50000

};

const safeUser = new Proxy(user, {

    get(target, property) {

        if (property === "salary") {

            if (target.role !== "admin") {
                return "Access denied";
            }

        }

        return Reflect.get(target, property);
    }

});

console.log(safeUser.name);
console.log(safeUser.salary);
```

Output:

```text
Shariar
Access denied
```

⚠️ এটি demonstration। Real application security-এর জন্য server-side authorization অবশ্যই প্রয়োজন।

---

# 28.48 — Reactive Systems-এর সাথে Connection

Proxy modern frontend architecture-এর কিছু concept বুঝতে সাহায্য করে।

ধরো:

```javascript
const state = {
    count: 0
};
```

Proxy দিয়ে change detect:

```javascript
const reactiveState = new Proxy(state, {

    set(target, property, value) {

        console.log(
            `${property} changed`
        );

        return Reflect.set(
            target,
            property,
            value
        );
    }

});

reactiveState.count = 1;
```

Output:

```text
count changed
```

এই ধরনের concept reactive systems/framework internals বোঝার ক্ষেত্রে গুরুত্বপূর্ণ।

---

# 28.49 — Symbol + Proxy একসাথে

Symbol দিয়ে internal-like metadata রাখা যায়:

```javascript
const metadata = Symbol("metadata");

const user = {

    name: "Shariar",

    [metadata]: {
        createdBy: "system"
    }

};

const proxy = new Proxy(user, {

    get(target, property) {

        if (property === metadata) {
            return Reflect.get(target, property);
        }

        return Reflect.get(target, property);
    }

});
```

এখানে Symbol unique key হিসেবে কাজ করছে।

---

# 28.50 — Proxy Performance Consideration

Proxy powerful হলেও সব জায়গায় ব্যবহার করা উচিত নয়।

কারণ proxy layer:

```text
property access
     ↓
trap
     ↓
target
```

অতিরিক্ত abstraction তৈরি করতে পারে।

তাই performance-sensitive code-এ অপ্রয়োজনীয় Proxy ব্যবহার avoid করা ভালো।

---

# 28.51 — Common Mistake: Symbol-কে Private ভাবা

ভুল ধারণা:

> Symbol মানেই private property।

❌ না।

Symbol property discover করা যায়:

```javascript
Object.getOwnPropertySymbols(obj)
```

অথবা:

```javascript
Reflect.ownKeys(obj)
```

তাই Symbol:

> **Unique property key**, security/privacy mechanism নয়।

---

# 28.52 — Common Mistake: `Symbol("id")` একই হবে ভাবা

```javascript
const a = Symbol("id");
const b = Symbol("id");

console.log(a === b);
```

Output:

```text
false
```

একই description ≠ same Symbol।

---

# 28.53 — Common Mistake: Proxy Target Automatically Protected

Proxy বানালেই original object protected হয় না।

```javascript
const user = {
    name: "Shariar"
};

const proxy = new Proxy(user, {});

user.name = "Ripon";
```

Original target সরাসরি পরিবর্তন করা সম্ভব।

---

# 28.54 — Common Mistake: `set` Trap-এ Boolean Return

`set` trap-এর ক্ষেত্রে সাধারণত success indicate করতে boolean return করতে হয়।

```javascript
set(target, property, value) {

    target[property] = value;

    return true;
}
```

---

# 28.55 — Common Mistake: Proxy-কে Security Boundary ভাবা

Proxy দিয়ে:

```javascript
salary access block
password property hide
```

করলেও client-side JavaScript-এর ওপর security নির্ভর করা উচিত নয়।

Real security:

```text
Frontend
   ↓
Backend Authorization
   ↓
Database
```

---

# 🔥 28.56 — MUST KNOW

### Symbol

```javascript
const id = Symbol("id");
```

### Symbol Property

```javascript
obj[id] = 100;
```

### Global Symbol

```javascript
Symbol.for("id");
```

### Well-known Symbol

```javascript
Symbol.iterator
```

### Proxy

```javascript
new Proxy(target, handler);
```

### Get trap

```javascript
get(target, property)
```

### Set trap

```javascript
set(target, property, value)
```

### Reflect

```javascript
Reflect.get()
Reflect.set()
Reflect.has()
Reflect.deleteProperty()
Reflect.ownKeys()
```

### Most important relationship

```text
Proxy → intercept
Reflect → perform operation
Symbol → customize/identify
```

---

# 🧠 Quick Cheat Sheet

```text
Symbol
│
├── Unique primitive
├── Unique property keys
├── Symbol.for()
├── Symbol.keyFor()
└── Well-known Symbols
       └── Symbol.iterator

Proxy
│
├── get
├── set
├── has
├── deleteProperty
├── ownKeys
├── apply
└── construct

Reflect
│
├── get
├── set
├── has
├── deleteProperty
├── ownKeys
├── apply
└── construct
```

সবচেয়ে গুরুত্বপূর্ণ:

```text
Symbol
   ↓
Special keys / behavior

Proxy
   ↓
Intercept behavior

Reflect
   ↓
Perform object operations
```

---

# 📝 Practice Set

## 🟢 Beginner

### Practice 1

একটি Symbol তৈরি করো:

```text
userId
```

এবং object-এর মধ্যে property হিসেবে ব্যবহার করো।

---

### Practice 2

দুটি:

```javascript
Symbol("id")
```

তৈরি করে check করো তারা equal কিনা।

---

### Practice 3

`Symbol.for()` দিয়ে:

```text
app.user
```

এর জন্য একটি global Symbol তৈরি করো।

---

# 🟡 Intermediate

### Practice 4

একটি object তৈরি করো যেটা:

```javascript
for...of
```

দিয়ে iterate করা যাবে।

`Symbol.iterator` অথবা generator ব্যবহার করতে পারো।

---

### Practice 5

Proxy দিয়ে এমন object বানাও যেখানে:

```text
age অবশ্যই number হবে
age negative হতে পারবে না
```

---

### Practice 6

Proxy দিয়ে প্রতিটি property read এবং write console-এ log করো।

Expected:

```text
GET → name
SET → age = 25
```

---

# 🔴 Advanced

### Practice 7

একটি **Read-only Proxy** তৈরি করো।

যে object-এর কোনো property পরিবর্তন করা যাবে না।

---

### Practice 8

একটি access-control Proxy তৈরি করো:

```text
user → name access
admin → salary access
```

---

### Practice 9

একটি custom class তৈরি করো যেখানে:

```text
Symbol.iterator
Proxy
Reflect
```

তিনটি concept একসাথে ব্যবহার করবে।

---

# 🎯 Final Mental Map

```text
                     JavaScript Metaprogramming
                              │
             ┌────────────────┼────────────────┐
             │                │                │
          Symbol            Proxy           Reflect
             │                │                │
       Unique Keys       Intercept Ops     Object Ops
             │                │                │
      ┌──────┴──────┐   ┌─────┴──────┐    ┌───┴────┐
      │             │   │            │    │        │
Symbol.iterator   Symbol.for   get/set   apply   get/set
      │                         │
      ↓                         ↓
   Iterable                  Validation
                            Logging
                            Access Control
                            Reactivity
```

## ⭐ এক লাইনে তিনটা মনে রাখো

> **Symbol → “আমার একটা unique key/behavior দরকার।”**

> **Proxy → “কেউ object-এর সাথে কী করছে সেটা intercept করতে চাই।”**

> **Reflect → “সেই object operation-টা standard way-তে perform করতে চাই।”**

### ✅ Chapter 28 Complete

এখন roadmap অনুযায়ী পরেরটি:

**Chapter 29 — JavaScript Execution Model & Event Loop**

এখানে আমরা JavaScript-এর সবচেয়ে গুরুত্বপূর্ণ internal concepts-এর মধ্যে কয়েকটি দেখব:

```text
Execution Context
Call Stack
Heap
Event Loop
Microtask Queue
Macrotask/Task Queue
Web APIs
Promise
setTimeout
async/await
Execution Order
```

এগুলো ভালোভাবে বুঝলে **JavaScript asynchronous code কেন এবং কীভাবে কাজ করে**—সেটা অনেক বেশি পরিষ্কার হয়ে যাবে।
